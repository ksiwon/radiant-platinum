// 설치 기록의 판 번호 하나 (IMPORT.md §8)
//
// ⚠️ **왜 파일이 따로인가.** 이 상수는 `manifestSchema`가 zod 리터럴로 쓰고
// `installer`가 기록을 만들 때도 쓴다. 같은 파일에 두면 **상수 하나 때문에
// 부팅 그래프가 zod를 통째로 끌어온다** — 첫 화면 예산에서 gzip 19.4kB다
// (실측, DEPLOY.md §2). 스키마는 필요할 때 받고, 번호만 여기 남긴다.
//
// ⚠️ 산출물의 모양은 이것이 아니라 `ASSET_FORMAT`이 센다 (`assetFormat.ts`).
// 둘을 하나로 두면 기록에 필드 하나 늘 때마다 사용자의 600MB가 날아간다

/** **기록의 모양** 판. 다르면 그 기록을 못 읽으니 다시 설치한다 */
export const CONTRACT_VERSION = 3
