// 화면 **왼쪽 위**에 붙는 것들이 서로 몇 픽셀을 비켜야 하는가.
//
// 계기판(`PerfOverlay`)과 조작 쪽지(`ControlHint`)가 같은 구석에 선다. 계기판은
// 개발 서버에서만 뜨고 접었다 펼 수도 있어서 **높이가 그때그때 다르다** — 그
// 값을 손으로 적어 두면 접는 순간 쪽지가 허공에 뜬다.
//
// 그래서 계기판이 제 아래끝을 재서 이 변수에 싣고, 쪽지는 그 아래에 붙는다.
// 계기판이 없으면 변수도 없고, 쪽지는 기본값(0)으로 제자리에 선다.

/** 왼쪽 위에서 이미 차지한 높이(px). 뿌리 요소에 실린다 */
export const HUD_LEFT_TOP = '--hud-left-top'
