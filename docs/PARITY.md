# 원작 대조 대장 — 플래티넘에 있는 것 / 우리에게 있는 것

이 문서는 **결손 목록**이다. "언젠가 하자"가 아니라 *무엇이 없고, 그것이
원작 어디에 있고, 무엇을 재면 되는가*를 적는다. 다 하면 이 문서는 빈다.

정본은 디컴프(`pret/pokeplatinum`)다. 짐작으로 적은 줄은 없다 — 각 줄의
"근거" 칸이 원작 파일이나 우리가 잰 수를 가리킨다.

- 진행 상태와 구현 노트는 [PLAN.md](PLAN.md), 자료 추출은 [DATA.md](DATA.md)가
  정본이다. 여기는 **차이만** 적는다.
- 원작에 **있는데 옮기면 안 되는 것**은 [BUGS.md](BUGS.md)에 따로 있다. 이 문서가
  "없는 것"이라면 그쪽은 "따라 만들지 않을 것"이다.
- ✅ 됨 · ◐ 반쯤 · ✖ 없음 · ⊘ 범위 밖 (§9에 이유)

---

## 0. 한눈에

| 갈래 | 됨 | 반쯤 | 없음 | 범위 밖 |
|---|---|---|---|---|
| 이야기·필드 (§1) | 23 | 1 | — | 1 |
| 배틀 (§2) | 19 | — | 1 | 2 |
| 포켓몬 개체 (§3) | 13 | — | — | 2 |
| 도구·가방 (§4) | 6 | 1 | 3 | 1 |
| 메뉴·화면 (§5) | 18 | 1 | 3 | 2 |
| 수집·특수출현 (§6) | 12 | — | — | — |
| 부가 시설 (§7) | 9 | 1 | 3 | 3 |
| 연출·소리 (§8) | 6 | 5 | — | — |

이 수는 아래 표에서 **세어 온 것**이다 (§7만 상태 칸이 없어 손으로 적었다).

**아직 없는 것은 일곱이다** — 사파리 배틀(§2.19) · 도구 등록 Y(§4.4) ·
우편(§4.8, 낱말 고르기가 딸려 있다) · 게임코너 경품(§4.10) ·
게임 기록·랭킹·TV(§7.5) · 복권(§7.6) · VS시커(§7.9).

**제일 큰 것부터 메운다.** 아래는 없으면 "완주할 수 있는 게임"이 아니었던 것들이다:

1. ✅ **진화** (§3.1) — 스물여섯 갈래가 다 돈다. 배틀 뒤에 장면이 열리고
   진화의돌로도 걸린다.
2. ✅ **걸음 계수기** (§1.1) — 독·리펠·친밀도가 원작 주기(4·1·128)로 돈다.
   알·꿀나무·레이더도 이 위에 얹으면 된다.
3. ◐ **필드 도구** (§4.1) — 스물다섯 갈래 중 여섯이 돈다(회복 38 · 기술머신
   100 · 진화의돌 10 · 리펠 3 · 탈출로프 · 자전거 = **152종**). 나머지는
   그 계통이 아직 없다는 것을 화면이 말해 준다.
4. ✅ **물 조우·낚시** (§1.5·§1.6) — 파도타기와 낚싯대 셋이 돈다. 시간대가
   슬롯 2·3을 갈아 끼운다.
5. ✅ **날짜 씨앗** (§6.11) — 하나가 넷을 연다: 빈티나 칸·무리·트로피가든·
   대습초원 일일. 조우표를 갈아 끼우는 세 자리가 안 겹쳐서 한 번에 다 걸린다.
6. ✅ **알·육성가** (§3.2·§3.3·§3.15) — 맡기고, 알이 생기고, 걸으면 깬다.
   개체값·성격·알 기술 유전까지 돈다.
7. ✅ **더블 배틀** (§2.2) — 트레이너 28명이 제 편성으로 열린다. 뷰가 쪽이
   아니라 자리 넷으로 접히고, 대상을 찍는 기술은 「누구에게?」를 묻는다.
8. ✅ **선두 특성·피리·리펠** (§1.22) — 리펠이 **실제로 막는다**. 그전에는
   걸음만 세고 아무것도 안 막았다. 특성 스무 종이 출현률·칸·레벨·성격·성별을
   민다. 원작 버그 넷은 [BUGS.md](BUGS.md)에 적고 **안 옮겼다**.
9. ✅ **어디서 왔는가와 요약 화면** (§3.8·§5) — 개체에 만난 자리·만난 날·
   원트레이너가 새겨지고, 요약 화면 넉 쪽이 그것으로 트레이너 메모를 쓴다.
   파티 화면의 갈래 메뉴도 원작 차례로 열린다.
10. ◐ **타운맵** (§5) — 공중날기가 목록이 아니라 **신오 지도 위**에서
    일어난다. 지도 그림도 격자 칸의 이름도 롬에서 뽑았다.

11. ✅ **폼** (§3.4) — 열세 종이 한 번호 아래 여러 모습을 갖는다. 개체에
    `form` 칸이 있고 종족값·타입·특성·아이콘·도트·3D·배틀이 그 값을 본다.
    도롱마담은 **싸운 땅**의 옷감을 입고, 조개무지는 맵이 정한 바다에서 나오고,
    안농은 유적 방마다 다른 글자가 나온다. 알도 여기서 제 그림을 얻었다(§8.10).

12. ✅ **배회 포켓몬** (§6.3) — 엠라이트와 크레세리아가 신오 도로 스물아홉 곳을
    돈다. **같은 개체가** 도망치고 다시 나온다 — 맞은 만큼을 들고, 개체값도
    그대로다. 걸어서 맵을 넘을 때만 움직이고, 방금 떠난 맵으로는 안 간다.

13. ✅ **기술 칸의 상성** (§2.22) — 기술 이름 밑에 「효과가 굉장함」이 뜬다.
    원작에 없고 BDSP에 있는 화면이라 그쪽을 따랐다. 잡았거나 쓰러뜨려 본
    종에게만 뜨고, 그 기록을 위해 도감에 칸이 하나 늘었다.

14. ✅ **모험노트** (§7.4) — 하루가 한 쪽이고 열 쪽이 남는다. 자리 일 넷 ·
    그날의 포켓몬 한 줄 · 트레이너 한 줄이라는 원작의 칸 수까지 옮겼고,
    겹치는 일을 거르는 규칙 일곱 갈래도 그대로다. 문장 103개가 전부 롬의 것이다.

15. ✅ **포켓치** (§7.3) — 앱 스물다섯이 든 시계가 **오른쪽 위 구석**에 걸린다.
    ⚠️ **배치만 BDSP를 따랐다** — 원작은 아래 화면을 통째로 쓰는 것이 전제고
    우리는 원 스크린이다. R로 키우고 접고, 길게 누르면 감춘다. 앱을 넘기면
    앞 앱이 쓰던 값이 사라지는 것까지 원작 그대로다(버퍼가 하나다).

16. ✅ **도감 나머지** (§5) — 검색·정렬·서식지·폼·전국도감이 붙었다.
    ⚠️ **정렬을 계산하지 않는다** — 롬이 구워 둔 목록 마흔일곱 벌을 그대로
    쓴다. 무게로 다시 정렬하면 **같은 무게끼리의 차례**가 달라지는데 493종
    중에 그런 짝이 여럿이다. 가나다순은 로케일마다 다른 목록이다.

17. ✅ **박스와 타운맵의 남은 것** (§5) — 박스가 다섯 갈래를 다 열고(도구
    옮기기·비교하기), 지도가 확대되고 표식이 깜빡이고 숨은 자리 넷이 열린다.

18. ✅ **깨어진 세계를 끝까지 내려간다** (§6.10) — 승강 발판이 층을 잇고
    바위 수수께끼가 풀리고, **밟으면 없던 블록이 실제로 나타난다**(네 층 42개).
    여기가 뚫려야 이야기를 끝낼 수 있었다.

19. ✅ **기술 연출이 원작 대본에서 나온다** (§2.13) — 468개 `anim.s`가 정한
    색·박자·흔들림·자리를 읽는다. 산술로 흔든 「서로 다른 값」이 아니다.

20. ✅ **규칙 구멍 셋** (§2.17 · §2.18 · §3.9) — 학습장치가 경험치를 반으로
    가르고, 남에게 받은 마리가 뱃지 수만큼만 말을 듣고, 포켓루스가 걸리고
    옮고 나으며 노력치를 두 배로 먹인다. 말 안 듣기는 NPC 교환 넷(§1.24)이
    붙으면서 **화면에 뜨게 됐다** — 남의 마리를 얻는 길이 그 넷뿐이다.

21. ✅ **동행·변장·다리** (§1.14~1.16) — 셰릴과 라이벌이 한 칸 뒤를 따라오고,
    닌자보이가 풀·바위·눈·모래 더미를 쓰고 숨어 있다가 눈이 마주치면 튀어나오고,
    다리는 **어귀를 밟았느냐**로 위와 밑이 갈린다. 마지막 것이 파도타기·자전거·
    낚시를 한꺼번에 고쳤다 — 다리 셋이 파도타기 목록에서 빠져 있어서 밑을
    지나는 순간 뭍으로 내려섰다.

22. ✅ **막혀 있던 길이 열렸다** (§1.23) — 리그의 승강판이 안 움직여서
    **사천왕에게 올라갈 수가 없었다.** 체육관 여섯도 장치가 곧 길인데 그
    장치가 없었다 — 물가시티의 물, 선단시티의 톱니, 영원시티의 꽃시계,
    운하시티의 뜨는 판, 장막시티의 샌드백, 연고시티의 문. 격자에는 그 길이
    안 적혀 있고 표가 대신 말한다.

23. ✅ **방지턱에 단차가 선다** (§1.11) — 원작 높이 자료가 720칸 다 평평해서
    3인칭에서는 잔디에 그은 선이었다. 나무·바위와 같은 갈래로 우리가 세운다.

24. ✅ **NPC 교환 넷** (§1.24) — 규칙 하나가 코드에만 있고 화면에는 없었다.
    통신교환이 없는 우리에게 남의 포켓몬이 들어오는 길이 이 넷뿐이라,
    말 안 듣기(§2.18)가 뜨는 것도 여기서다.

25. ✅ **이야기 길목의 빈 자리 셋** (§1.25 · §10) — 안 만든 명령은 멈추지
    않고 **건너뛴다.** 그래서 게임이 서지는 않았지만 조용히 아무 일도 안
    일어났다: 갤럭시단아지트에 호수 삼신을 가둔 통이 안 서고, 사천왕 넷과
    챔피언 앞에서 눈이 마주치는 곡이 안 났다. 맵별로 다시 세어 이야기 길목에
    걸친 자리를 골라냈다 (`.audit/probe/mapGap.test.ts`).

26. ✅ **포켓몬레이더** (§6.5) — 사슬이 길수록 색이 다를 확률이 오른다. 흔들림
    두 가지가 「이 무더기가 사슬을 이어 줄까」를 미리 말해 주는 것까지 옮겼다.

27. ✅ **나무열매 밭 118** (§4.6) — 도로변의 밭이 **새 리포트부터 열려 있다.**
    시간이 밭마다 따로 흐르고 껐다 켠 사이에도 흐른다. ⚠️ **본 적 없는 밭은
    안 자란다** — 원작이 「화면에 들어왔는가」로만 그 시계를 켠다. 가방에서
    도구를 고르는 화면도 여기서 붙었다 (`OpenBag`·`GetSelectedItem`) —
    비료와 심을 열매를 그 화면으로 고른다.

**다음**: 사파리·대습초원 (§7.7).

**뒤로 미룬 것**: 우편과 낱말 고르기(§4.8 · §5 `easy_chat`). 우편은 장식이고
낱말이 먹이는 곳은 우편과 통신(§9)뿐이라 아무 길목도 안 막는다 — 계통이 큰
데 비해(우편함 20통 · 낱말 무리 14 · 낱말 뱅크 일곱 · 편지지 12종) 여는 문이
없어서 다른 것들 뒤로 보냈다.

---

## 1. 이야기·필드

| | 원작 | 우리 | 근거 · 다음에 할 일 |
|---|---|---|---|
| 1.1 걸음 계수기 | ✅ | `FieldSystem`이 한 칸마다 콜백을 돌린다 — 독·친밀도·알·리펠·사파리·레이더·상호교류광장 | `engine/actor/steps.ts` + `scene/stepSystem.ts`. 차례도 원작 그대로다 — 독 → 리펠 → 친밀도 |
| 1.2 필드 독 데미지 | ✅ | 4걸음마다 1, 1에서 멈춘다 | 알리는 것도 우리 문장이 아니라 **원작 공용 스크립트 2003**을 돌린다. 1까지 내려간 마리는 친밀도를 잃는다 |
| 1.3 리펠 | ✅ | 약한 야생을 실제로 막는다 | 100·200·250걸음이 롬의 `effectParam`이다. 끝나면 공용 스크립트 2032. 막는 기준은 선두가 아니라 **싸울 수 있는 첫 마리**의 레벨이고, 조건이 `>`라 **같은 레벨은 안 막힌다** (§1.22) |
| 1.4 야생 조우 — 땅 | ✅ | 관문 셋 + 유예 구간 | `engine/battle/encounter.ts` |
| 1.5 야생 조우 — 물·낚시 | ✅ | 파도타기 5슬롯·낚싯대 3종·빈티나 | 조우 칸이 **탈 수 있는 물 열여섯 중 여섯**이라 폭포·다리 위는 안 나온다. 낚시는 창 45/30/15프레임에 표도 가중치도 낚싯대마다 다르다 (`engine/actor/fishing.ts`). 천관산 B1F의 빈티나 네 칸은 날마다 바뀌고, 칸을 맞춰도 **절반은 그냥 떨어진다** |
| 1.6 야생 조우 — 시간대 | ✅ | 슬롯 2·3이 아침/낮/밤에 갈린다 | `timedLand()`. 낮·해질녘 → `day`, 밤·심야 → `night`, 아침은 표 그대로. **종족만 바뀌고 레벨은 안 바뀐다** |
| 1.7 야생 조우 — 무리·레이더 | ✅ | `swarm`(슬롯 0·1)·`radar` 슬롯 | 무리는 된다(§6.4). 갈아 끼우는 세 자리가 안 겹친다 — 무리 0·1, 시간대 2·3, 트로피가든 6·7. 레이더는 §6.5가 붙으면서 같이 돈다 — 칸 4·5·10·11이다 |
| 1.8 비전기술 아홉 | ✅ | 자격·자리 판정 둘 다 | `engine/script/fieldMoves.ts` — 뱃지 짝이 원작 그대로다 |
| 1.9 자전거 | ✅ | 두 단 | `engine/actor/bike.ts` |
| 1.10 워프·계단·문 | ✅ | | `disarmWarp`까지 |
| 1.11 방지턱 | ✅ | 뛰어내리고 단차가 선다 | ⚠️ **원작 높이 자료에는 단차가 없다** — 720칸이 720칸 다 양쪽이 같은 높이고 턱은 바닥 그림(`allpeak`)이다. 고정 부감에서는 그림만으로 읽혔지만 3인칭에서는 잔디에 그은 갈색 선이 된다. 그래서 나무·바위와 같은 갈래로 **우리가 세운다** (`scene/Ledges` — 칸마다 높이 0.18짜리 판 + 뛰어내리는 쪽 앞면). 뛰는 규칙은 그대로 거동값이 정한다 (`actor/ledge`) |
| 1.12 NPC 이동 유형 68 | ✅ | 717명이 혼자 움직인다 | `engine/actor/ambient.ts` |
| 1.13 시선 배틀 | ✅ | | `engine/actor/sight.ts` |
| 1.14 따라다니는 사람 | ✅ | 동행 여덟 자리 + 짝 트레이너 셋 | `actor/ambient`의 `follow`·`partner`. **주인공이 지금 선 칸이 아니라 방금 떠난 칸으로 간다** (`sub_02069D8C`) — 지금 칸으로 걸으면 두 몸이 겹치고 주인공이 멈추는 순간 옆으로 파고든다. 모퉁이에서는 좌우를 먼저 좁힌다(`GetDirectionBetweenPoints`). ⚠️ **배치표에 `FOLLOW_PLAYER`는 한 명도 없다** — 스크립트가 그 자리에서 유형을 갈아 끼운다(영원의숲 셰릴 · 강철섬 리키 · 유턴동굴 미라 · 챔피언로드 마리 · 스타크산 벅 · 아뮤티광장 · 201번도로 라이벌 둘). 그래서 `SetMovementType`이 **배치표가 아니라 서 있는 사람**을 고쳐야 한다 — 그동안은 「맵을 나갔다 들어와야」 먹었다. 걸음 빠르기는 주인공 속도에서 고른다(`playerSpeed`) — 원작은 주인공의 이동 동작을 그대로 베끼는데 우리 주인공은 동작이 아니라 속도로 움직인다 |
| 1.15 변장한 트레이너 | ✅ | 11명 (풀 5 · 바위 3 · 눈 2 · 모래 1) | 닌자보이 열과 검은띠 하나가 이동 유형 51~54로 숨어 있다. 원작은 사람을 32단위 땅 아래로 내리고 그 자리에 더미 모델을 그린다(`sub_0206A0BC` + `ov5_021F3D90`) — 우리는 사람을 안 그리고 더미만 그린다(`scene/DisguisePlates`). ⚠️ **더미 모델을 새로 안 만들었다** — `fldeff.narc` 103~106이 넷 다 꼭짓점 넷·삼각형 둘짜리 **한 칸 크기의 평면**이고(y = 0.1875 · 양면 · 16×16 그림) 그 값을 롬에서 실측해 옮겼다. 정체는 시야에 걸리거나 말을 거는 순간 드러나고, 그때 이동 유형이 `NONE`으로 갈린다(`ApproachingTrainerTask_SwitchMovementTypeNone`) — 「드러났는가」를 따로 안 들고 있어도 되는 이유다. 시야는 사방(`TRAINER_TYPE_VIEW_ALL_DIRECTIONS`)이라 이미 §1.13이 맡고 있었다 |
| 1.16 다리 밑 통과 | ✅ | 다리 1,980칸 · 어귀 158칸 | `actor/bridge`. **들어온 길이 층을 정한다** — 어귀(`BRIDGE_START`)를 밟아야 「위」가 되고, 다리가 아닌 칸을 밟으면 풀린다(`MapObject_DetermineElevatedBridgeStatus`). 그 한 값으로 같은 칸이 물이 되기도 땅이 되기도 한다(`MapObject_IsOnWater`): 위를 건너면 걸어가고 밑을 지나면 파도를 탄다. ⚠️ **파도타기 목록에서 다리 셋(0x73·0x78·0x7c)이 빠져 있었다** — 원작 거동표에는 `SURFABLE`이 서 있다. 빼는 것으로 「위를 건넌다」를 흉내 내고 있었고, 그래서 밑을 지나는 순간 파도타기가 풀렸다. 같은 층 판정이 자전거 내리기(`CanUseBicycle`)와 낚시(`CanUseFishingRod`)도 가른다. ⚠️ **자전거 다리 거동값도 틀려 있었다** — 0x76~0x7d인데 0x74·0x75(모래·눈 위의 보통 다리)를 넣고 있었다. 높이는 BDHC 판이 겹치는 자리에서 지금 높이로 고르므로(`map/height`) 이미 맞았다 |
| 1.17 스크립트 명령 | ◐ | 840종 | 425종 구현, 닿는 자리의 **97.9%**가 돈다(범위 안으로는 98.8%). 나머지 1,178자리는 §10에 계통별로 갈라 뒀다 |
| 1.18 초기화 표 | ✅ | 맵마다 둘째 스크립트 | 오프닝이 끝까지 돈다 |
| 1.19 통신 필드(유니온룸) | ⊘ | | §9 |
| 1.20 왕복 대사·간판 | ✅ | 간판 판 넷 중 판 둘 + 대사창 둘 | 원작 배분 그대로 |
| 1.21 포켓몬센터·프렌들리숍 | ✅ | 회복·구매·판매 | `scene/pokecenter.ts`, `engine/bag/mart.ts` |
| 1.23 **맵마다 움직이는 장치** | ✅ | 승강판 9 · 체육관 6 · 승강기 | ⚠️ **여기가 막혀서 게임을 못 끝냈다.** 사천왕과 챔피언에게 올라가는 길이 계단이 아니라 리그의 승강판이고, 체육관 여섯은 장치가 곧 길이다 — 격자에는 그 길이 안 적혀 있다. 원작이 「이 맵에는 이런 장치가 있다」를 갈래 열하나로 못 박고(`dynamic_map_features.c`) 통행 판정·프레임 갱신을 그리로 보낸다. **한 번에 하나만** 산다. 옮긴 것: 승강판 아홉(`world/platformLift` — 들어온 문의 z가 층을 정한다) · 물가시티 물바닥(거동값 0x56~0x59) · 선단시티 톱니 셋·여섯·열셋(상자 12·18·40과 회전 상태 넷의 표) · 영원시티 꽃시계(13×13 표 다섯 + 시침 뛰어넘기) · 운하시티 뜨는 판 스물넷(층마다 32×32 표) · 장막시티 샌드백 아홉·타이어 열하나 · 연고시티 문 고르기(틀린 문의 **목적지**를 되돌린다) · 승강기 열여덟 자리. ⚠️ **딛는 면이 움직이는 자리를 위해 BDHC 위에 판을 한 겹 얹었다**(`map/dynamicHeight`) — 겹치면 판이 바닥보다 낮을 때는 늘 바닥이고, 높으면 지금 높이에 가까운 쪽이다. ⚠️ 연고시티의 힌트 그림(`ov5_021F4CEC`)과 DP 시절 승강판 둘(플래티넘에서 안 닿는 맵)은 아직 없다 |
| 1.24 **NPC 교환** | ✅ | 넷 · 장면 · 말 안 듣기의 문 | 무쇠시티 케이케이(아브라 ↔ 알통몬) · 영원시티 조자리(페라페 ↔ 브이젤) · 눈설시티 고옹이(고우스트 ↔ 요가램) · 226번수로 킹킹이(잉어킹 ↔ 케이시). ⚠️ **통신교환이 없는 우리에게 남의 포켓몬이 들어오는 길이 이 넷뿐이라, §2.18 말 안 듣기가 게임 안에서 보이는 것도 여기서다.** 표는 `fld_trade.narc` 넷(u32 스무 칸)이고 이름 여덟은 뱅크 370이다. ⚠️ **표에 레벨이 없다** — 받는 마리의 레벨은 내가 건네는 마리의 것이다. ⚠️ **만난 자리가 지금 맵이 아니라 「통신교환」(2001)이다** — 원작이 맵 이름을 넘기지만 `sel == 1` 가지가 그 인자를 안 쓴다. 개체값·성격값·트레이너 번호가 다 고정이라 **넷 다 늘 같은 개체**고 색도 안 다르다(원작이 그 자리에서 단언한다). 장면은 뱅크 350의 네 줄과 원작 마디·소리 그대로다(`ui/menu/TradeScreen`) — 통신관 그림 다섯 마디만 한 마디로 묶었다. ⚠️ **언어 칸은 아직 개체에 없다** — 잉어킹이 독일어인 것이 국제교배(다른 언어면 색 판정을 네 번 더 굴린다, `Egg_SetInitialData`)와 도감 언어 판정의 근거인데, 우리는 게임 언어를 설정에서 바꿀 수 있어서 「이 리포트의 언어」가 원작처럼 하나로 안 정해진다. 표에는 담아 두었다 |
| 1.25 **호수 삼신의 감금장치** | ✅ | 통 셋 · 여는 차례 | 갤럭시단아지트 관제실(494)에 통 셋이 서고 시로나가 오면 열린다 (`world/lakeGuardianUnits` · `scene/lakeGuardianUnits`). ⚠️ **명령 둘이 없어서 이야기 한복판이 빈 방이었다** — 통을 세우는 자리도 푸는 자리도 그냥 건너뛰고 있었다. 소품 번호 496은 배치표에만 있고 BG 이벤트가 안 붙어 있다(꿀 나무와 같다). ⚠️ **이미 풀어 준 리포트면 열린 채로 선다** — 원작이 여는 애니를 **마지막 프레임으로 보내** 놓는 것이 그것이다. 안 하면 다시 들어왔을 때 도로 닫혀 있다. ⚠️ **푸는 자리는 스크립트를 세운다**(`return TRUE`) — 안 세우면 통이 열리기 전에 삼신이 날아간다. ⚠️ **여는 그림은 우리 것이다** — 원작의 애니 둘은 소품에 딸린 nsbca고 우리 파이프라인은 정지 메시만 굽는다(§6.6과 같은 처지). **상태 기계와 두 마디**(0번이 끝나기를 기다렸다가 1번을 한 번 돌린다)는 그대로 옮겼다 |
| 1.22 **선두 특성·피리·리펠 보정** | ✅ | 특성 **20종** · 피리 둘 · 클리어부적 부류 · 리펠 | `engine/battle/encounterLead.ts`. 출현률(개미지옥·노가드·발광 ×2, 하얀연기·속보·악취 ÷2, 모래숨기·눈숨기는 날씨를 봐야 ÷2) → **100으로 자르고** → 피리(검은 ÷2 · 하얀 ×1.5) → 클리어부적·순수한향로 ×2/3 차례다. 칸 고르기(자력=강철·정전기=전기), 레벨 밀기(의욕·의기양양·프레셔), 쫓아내기(날카로운눈·위협), 성격(싱크로), 성별(헤롱헤롱바디 **2/3**), 도구 확률(복안 45/50/5 → 20/60/20)까지 돈다. ⚠️ 원작 버그 둘은 **안 옮긴다** — 끈끈이·흡반의 낚시 ×2와 물에서의 자력([BUGS](BUGS.md) §1.2·§1.3) |

## 2. 배틀

| | 원작 | 우리 | 근거 · 다음에 할 일 |
|---|---|---|---|
| 2.1 싱글 배틀 | ✅ | | @pkmn/sim gen4 + 우리 표. 무작위 1,500판 담금질 통과. ⚠️ **배틀이 파티를 낫게 하지 않는다** — sim의 팀 목록에는 PP처럼 체력 칸도 없어서 무엇을 넣든 만피·무상태로 서고, 그 값이 끝나면서 세이브에 도로 적혔다(7 남은 모부기가 55로 서서 55로 돌아왔다). PP와 같은 자리에서 남은 체력·상태이상·잠든 턴을 밀어 넣는다(`BattleSession.syncVitals`). 쓰러진 마리는 앞세우지 않는다 — 원작도 의식이 있는 첫 마리를 내보낸다 |
| 2.2 **더블 배틀** | ✅ | 트레이너 **28명** | `trainers.json`에서 실측 28/928. 뷰가 쪽이 아니라 **자리**(`p1a`·`p1b`·`p2a`·`p2b`)로 접힌다 — 쪽으로 접으면 둘째 마리의 체력이 첫째를 덮는다. 명령은 자리마다 묻고 **한 줄로 묶어** 보낸다(`p1 move 1 1, move 2 -1`). 대상을 찍어야 하는 겨냥 갈래는 열넷 중 **다섯**이고, 눈으로 고르지 않고 sim에 하나씩 던져 거절당하는 것을 셌다. 무대도 발판 넷을 세운다. ⚠️ 남은 것 셋: 야생 더블(AI 파트너)·AI의 **대상 고르기**(지금은 합법 후보 중 무작위다 — 원작 AI는 대상까지 점수를 매긴다)·더블 자리의 BDSP 좌표(우리가 정한 ±1.7이다) |
| 2.3 트레이너 AI | ✅ | 점수 매기기 + 플래그 | `engine/battle/ai` — 원작 스크립트를 옮겼다 |
| 2.4 트레이너 가방 | ✅ | | §7.7.7 |
| 2.5 내 가방 | ✅ | 68종 | §7.8 |
| 2.6 교체 화면 | ✅ | | |
| 2.7 포획 | ✅ | 볼 종류별 배수 | |
| 2.8 도망 | ✅ | | |
| 2.9 경험치·노력치·레벨업 | ✅ | | `meta/reward.ts` |
| 2.10 기술 네 칸이 찼을 때 | ✅ | "무엇을 잊게 할까?" 화면 | 배틀 **안에서** 묻는다 — 재생기가 그 박자에서 서고(`Beat.ask`) 답할 때까지 안 넘어간다 |
| 2.11 진화 (배틀 후) | ✅ | 레벨이 오른 자리만 본다 | 원작의 `leveledUpMonsMask` 그대로. 이긴·잡은·도망친 판에서만 열린다 |
| 2.12 상금 | ✅ | 분류별 배수표 | |
| 2.13 기술 연출 | ✅ | 468개 | **기술마다 원작 대본을 읽는다** (`res/moves/<이름>/anim.s`, 12,514줄 → `moveAnim.json`). 배경 물들임 108(색 13가지) · 몸 물들임 153 · 몸 떨림 279 · 화면 흔들림 30 · 달려 나감 36 · 포물선 22 · 눌림 24 · 사라짐 12 · 입자 붙임 1,742자리. 도형·궤적의 틀 다섯은 `battle/vfx`가, 대본 값은 `scene/battle/moveElements`가, 몸에 거는 것은 `stageRefs.moveImpact`를 거쳐 무대가 맡는다. ⚠️ **한때 `(id*7)%11` 산술이었다** — 「471개가 다 다르다」는 초록으로 통과하는데 화면은 전부 같았다. 그래서 시험이 이제 「배경 색이 13가지로 **뭉치는지**」를 본다. ⚠️ **연출의 높이가 상수였다** — 줄기도 덩어리도 y=1.2에서 나가고 발밑 고리는 반지름 1.5였는데, 화면에 서는 키는 디그다 0.30m부터 갸라도스 3.54m까지다. 작은 쪽은 머리 위 네 배 높이로 빔이 지나갔고 큰 쪽은 배를 뚫고 지나갔다. 지금은 무대가 반 초에 한 번 재는 **그 자리에 선 몸의 키**로 입(0.78)·몸통(0.55)·둘레(0.45)를 잡고 도형 크기도 두 몸의 평균에서 나온다 (`scene/battle/moveAnchor`). ⚠️ 입자 알갱이(.spa)는 아직 안 읽는다 |
| 2.14 배틀 무대 | ✅ | 서른 벌 | `battleBG` |
| 2.15 시합규칙 「교체」 | ✅ | | |
| 2.16 첫 배틀(급소 없음) | ✅ | | |
| 2.17 학습장치 | ✅ | 경험치 절반을 안 나간 마리에게 | `battle/meta/reward.ts`의 `expPool`+`expFor`. **든 마리가 하나라도 있으면 경험치가 반으로 갈린다** — 나간 마리들이 한쪽 절반을, 든 마리들이 다른 절반을 나눠 갖고, 둘 다인 마리는 양쪽을 받는다 (`BtlCmd_CalcExpGain`). ⚠️ **배수는 몫을 만든 뒤 받는 마리마다 곱한다** — 럭키에그 → 트레이너전 → 교환 차례로, 매번 버림이다. 몫에 미리 곱하면 남의 럭키에그로 불어난 경험치를 학습장치가 받는다. ⚠️ 레벨 100은 인원 수에는 들어가면서 한 점도 안 받는다(원작이 인원을 셀 때 레벨을 안 본다) — 걸러 내면 다 큰 마리를 데리고 다니는 것이 이득이 된다 |
| 2.18 말 안 듣기 | ✅ | 교환 개체 + 뱃지 수 | `battle/meta/obedience.ts`. 문턱은 뱃지 0·2·4·6에서 10·30·50·70으로 오르고 여덟이면 사라진다. **판정을 두 번 굴린다** — `((rand&0xFF)*(레벨+문턱))>>8 < 문턱`. 떨어지면 다른 기술을 쓰거나(`otherMove`), 문턱을 넘긴 폭이 그대로 확률이 되어 졸거나(`nap`) 자기를 때리거나(`hitSelf`) 아무것도 안 한다(네 마디). 불복은 볼·도망과 같은 빈 턴 칸으로 턴을 버린다 — 그래야 상대가 제 수를 둔다. ⚠️ **턴을 못 비우는 자리(참기·역린·도발)에서는 그냥 듣는다** — 명령을 안 보내면 sim이 답을 기다리다 배틀이 선다. 걸릴 개체는 NPC 교환 넷이 준다 (§1.24) — 우리에게 남의 마리를 얻는 길이 그것뿐이다 |
| 2.19 사파리 배틀 | ✖ | 돌·미끼·도망 | §7 |
| 2.20 배틀타워·프론티어 | ◐ | 배틀팩토리 | **배틀팩토리 하나를 만들었다** (§9.3) — 빌린 여섯에서 셋을 골라 일곱 판을 잇고, 이길 때마다 쓰러뜨린 상대와 한 마리를 바꾼다. 나머지 넷(타워·스테이지·캐슬·룰렛)은 §9. ⚠️ **멀티도 §9다** — 짝이 있어야 성립한다 |
| 2.21 통신 배틀 | ⊘ | | §9 |
| 2.22 **기술 칸의 상성** | ✅ | BDSP에만 있다 | 기술 이름 밑에 「효과가 굉장함 · 별로임 · 없음」이 뜬다 (`engine/battle/movePreview`). ⚠️ **원작에 없는 화면이다** — 두 화면짜리 DS는 아래 화면이 기술 넷으로 꽉 차서 넣을 자리가 없었고 7세대에서 처음 붙었다. 우리는 화면이 하나이므로 BDSP 쪽을 따른다. **잡았거나 쓰러뜨려 본 종에게만** 뜬다(도감에 `battled` 칸이 생겼다, 판 14) — 처음 보는 것의 약점을 공짜로 주지 않는다. 변화 기술과 발버둥에는 안 뜨고, 잠재파워는 **개체값이 정한 타입**으로 잰다(`BtlCmd_CalcHiddenPowerParams`). 상성표는 AI가 쓰던 것과 같은 것이라(`ai/typeChart`) 324쌍이 sim과 대조되어 있다. ⚠️ 배틀에 선 것을 도감에 적는 자리도 여기서 생겼다(`BattleSystem_DexFlagSeen`) — 그전에는 풀숲에서 백 마리를 만나도 도감이 비어 있었다 |

## 3. 포켓몬 개체

| | 원작 | 우리 | 근거 · 다음에 할 일 |
|---|---|---|---|
| 3.1 진화 | ✅ | 26가지 방법 | 246갈래가 다 걸린다(`engine/pokemon/evolution.ts`). 시험이 **스물여섯 갈래를 하나씩 밟아** 안 걸리는 것이 없음을 센다. ⚠️ **두 갈래는 원작 그대로면 영영 안 걸린다** — 아름다움(빈티나)은 콘테스트가, 교환(**열여섯 종·열일곱 갈래**)은 통신이 여는 것이고 둘 다 범위 밖이다(§9). 문턱을 낮추는 대신 **도구로 대신한다** (§12.2) — 그냥 교환 넷은 연결의 끈이, 지닌 채 교환하던 열셋은 **그 지닌 도구를 쓰는 것**이 연다. 종족 표는 안 고치고 거는 쪽만 바꾼다. ⚠️ **진화 큐가 「무엇이 걸었는가」를 같이 든다** (`state/evolutionStore`) — 자리 번호만 들면 화면이 다시 판단할 때 레벨업으로만 볼 수밖에 없고, 그러면 **도구로 거는 진화가 통째로 죽는다**(돌은 사라지는데 아무 일도 안 난다). 원작도 진화 호출에 `context`를 같이 넘긴다 |
| 3.2 **알·부화** | ✅ | 걸음 수 · 알 그룹 · 유전 | `engine/pokemon/breeding.ts`. 남은 부화 걸음은 **친밀도 칸을 같이 쓴다**(원작이 그렇다). 알 주기 255걸음(열두 날만 230)마다 1씩, 마그마의무장·불꽃몸이 있으면 2씩 깎인다 — **알은 안 센다**. 깨면 친밀도가 종족 기본값이 아니라 120이다 |
| 3.3 육성가 | ✅ | 맡기기·레벨·알 받기 | 두 자리 + 알 하나가 세이브에 있고 명령 열셋이 돈다. 궁합 0·20·50·70과 말투 네 단계(**0이 제일 좋다**), 걸음 하나가 경험치 1, 요금은 오른 레벨 × 100 + 100. 알은 **256걸음에 한 번만** 판정한다 |
| 3.4 **폼 체인지** | ✅ | 열세 종 · 폼 79가지 | 개체에 `form` 칸이 있다(판 13). 종족값·타입·특성·기술표가 `pl_personal`의 폼 칸(496~507)에서 오고, 배틀이 sim에 폼 이름으로 넘긴다. 야생은 맵이 정하고(조개무지 서/동 바다 · 안농 방마다 다른 글자), 도롱마담은 **싸운 땅**이, 기라티나는 백금옥이, 쉐이미는 그라시데아가, 로토무는 가전이 정한다. 그림도 다 있다 — 아이콘 540칸 · 배틀 도트 130장(`pl_otherpoke`) · 3D 모델 64개. ⚠️ **배틀 안에서만 바뀌는 폼은 화면이 못 따라간다** — 날씨구슬 캐스퐁과 플라워기프트 체리버가 그렇고, 규칙(타입 바뀜)은 sim이 제대로 돌린다 |
| 3.5 친밀도 | ✅ | 걸음·레벨업·기절 | 128걸음마다, **개체마다 동전을 던져** 절반을 버린다(`LCRNG_Next() & 1`). 럭셔리볼·평온의방울 보정도 붙는다 |
| 3.6 성격·특성·성별·이로치 | ✅ | PID에서 | `instance.ts` |
| 3.15 알 유전 | ✅ | 개체값 3 · 성격 · 알 기술 · 향로 · 볼테커 | 변함없는돌은 4세대 사양대로 **반쯤만** 먹는다. ⚠️ 개체값 유전의 원작 버그는 **안 옮겼다** — 뽑은 자리 대신 루프 변수를 지우는 자기 모순이라 셋을 늘 서로 다르게 뽑는다 ([BUGS](BUGS.md) §1.4) |
| 3.7 개체값·노력치 | ✅ | | |
| 3.8 잡은 자리·잡은 레벨 | ✅ | 만난 자리·만난 날·원트레이너·알 자리 | `engine/pokemon/origin.ts`. 원작 갈래 일곱 중 셋을 옮겼다 — 0 잡았다·받았다 · 3 알을 받았다 · 6 부화했다. **알 자리와 만난 자리가 서로를 지운다**(`ResetMetLocationAndDate`) — 안 지우면 잡은 마리가 알에서 나온 것으로 읽힌다. ⚠️ 판 11 이전 리포트는 자리가 0이라 원작 자신의 대답인 「수수께끼의 장소」로 뜨고 날짜 줄이 비어 있다 — 없는 과거를 지어내지 않는다. 나머지 넷(통신교환·팔파크·이벤트 배포·통신 알)은 §9 밖 |
| 3.9 포켓루스 | ✅ | 감염 3/65536 · 전염 · 노력치 2배 | `engine/pokemon/pokerus.ts`. 한 바이트가 둘을 담는다 — 아래 니블이 남은 날(1~4), 위 니블이 균주. 배틀이 끝날 때마다 `LCRNG_Next()`가 16384·32768·49152 중 하나면 한 마리가 걸리고, 이어서 **세 번에 한 번** 옆 칸으로 옮는다(`Pokemon_ValidatePokerus`). 날이 넘어가면 그만큼 깎이고 **나흘을 넘겼으면 통째로 낫는다**(`stepSystem.checkDay`). ⚠️ **나아도 노력치 두 배는 안 사라진다** — 원작의 `Pokemon_HasPokerus`가 바이트가 0이 아니기만 하면 참을 낸다. 「남은 날」로 판정하면 나흘 뒤에 조용히 사라진다. 요약 화면이 걸린 동안 딱지를, 나은 뒤에는 점을 보여 준다 |
| 3.10 리본 | ⊘ | 다수 | §9. 저장할 칸도 보여 줄 화면도 안 만든다. ⚠️ **그래도 명령 일곱은 만들었다** — 읽는 넷이 0을, 쓰는 셋이 아무 일도 안 한다. 안 만들면 결과 변수가 앞 갈래의 값으로 남아 스크립트가 우연히 갈라진다. ⚠️ **리본신드롬은 그 0으로 닫힌다** — 우리가 문을 막는 것이 아니라 1층 들머리가 제 대사로 돌려보낸다 (§9.2) |
| 3.11 콘테스트 능력치 | ⊘ | 멋짐·아름다움… | §9. 아름다움이 빈티나 진화를 열던 자리는 §12가 도구로 대신한다. **장식만 따로 남겼다** (§7.16) |
| 3.12 보관 시스템 18×30 | ✅ | 맡기기·꺼내기·정리 | `engine/pokemon/boxes.ts` |
| 3.13 별명 | ✅ | | |
| 3.14 도구 지니기 | ✅ | 주고 · 뺏고 · 맞바꾼다 | 파티 화면의 갈래 메뉴 「지닌물건」이 `PartyMenu_SelectItem`의 셋을 그대로 연다. 이미 들고 있으면 **맞바꾼다** — 덮어쓰면 도구 하나가 사라진다 |

## 4. 도구·가방

| | 원작 | 우리 | 근거 · 다음에 할 일 |
|---|---|---|---|
| 4.1 필드에서 도구 쓰기 | ◐ | `sItemUseFuncs` — 회복 · 타운맵 · 탐험키트 · 자전거 · 모험노트 · 기술머신 · 편지 · 나무열매 · 포핀케이스 · 팔패드 · 레이더 · 스프레이덕 · 비료 · 꿀 · VS시커 · 낚싯대 3 · 진화의돌 · 탈출로프 · 하늘의피리 · VS레코더 · 그라시데아 | `item_use_functions.c`의 스물다섯 갈래를 표로 옮겼다(`engine/bag/fieldUse.ts`). **여덟이 돈다** — 회복·기술머신·진화의돌·리펠·**피리 둘**·탈출로프·자전거·**포켓몬레이더**(155종). 나머지는 "무엇이 없어서 못 쓰는지"를 말한다. ⚠️ **진화의돌 칸이 10이 아니라 23이다** — 롬의 돌 열에 우리 도구 둘과 **지닌 채 교환하던 열하나**가 붙는다 (§12.2). 그 열하나는 원작의 갈래가 0(밖에서 못 쓴다)이라 표만 보면 다 막히므로, 목록을 종족표에서 뽑아 넘긴다 |
| 4.2 주머니 8개 · 상한 | ✅ | | `engine/bag/bag.ts` |
| 4.3 사고팔기 | ✅ | | `engine/bag/mart.ts` |
| 4.4 도구 등록(Y 버튼) | ✅ | 소중한 물건 하나를 단축키로 | 가방에서 **Y로 켜고 끄고**, 필드에서 **Y로 바로 쓴다** (`scene/registeredItem`). 등록할 수 있는 것은 롬 표의 비트가 정한다(`ITEM_PARAM_CAN_REGISTER`) — **열아홉 가지고 전부 소중한 물건이며 전부 못 버리는 것**이다. ⚠️ **원작은 가방의 갈래 메뉴에서 「등록한다」를 고른다** — 우리 가방에는 아직 그 메뉴가 없어서 Y로 대신한다(나무열매 태그를 Tab으로 여는 것과 같은 결정이고, 여는 길만 다르다). ⚠️ **쓰는 자리를 한 벌로 모았다** (`ui/menu/itemAction`) — 가방에서 고르는 길과 Y로 쓰는 길이 갈라지면 자전거가 가방에서만 굴러간다. ⚠️ **쓰기 직전에 가방을 다시 센다** — 등록만 해 두고 버릴 수 있다. 리포트에 칸 하나(판 30, `registeredItem`), 원작도 가방에 u16 하나뿐이라 **여럿은 못 등록한다** |
| 4.5 기술머신 92 + 비전머신 8 | ✅ | 가르치기 | 무엇을 가르치는지는 롬 아이템 자료에 **없어서** 디컴프에서 굽는다(`TM_MOVES`). 배울 수 있는지는 종족표의 128비트가 정한다 |
| 4.6 **나무열매 심기·물주기·수확** | ✅ | 밭 118곳 | `berry_patches.c`. 배치표의 밭 객체 118개가 `data[0]`에 밭 번호를 들고 있고, 그 자리가 새 리포트부터 열매가 심긴 채로 시작한다 (`berryInit`의 118줄, 디컴프 전용). ⚠️ **본 적 없는 밭은 안 자란다** — `isGrowing`을 세우는 자리가 「화면에 밭이 들어왔는가」 하나뿐이라 (`BerryPatches_UpdateGrowthStates`) 가 본 밭만 시간이 흐른다. ⚠️ **안 따고 놔두면 열매를 잃는다** — 열린 단계가 끝나면 개수가 0이 되고 싹으로 되돌아간다. ⚠️ **아홉 날쯤 안 오면 밭이 통째로 비워진다** (단계 길이 × 총 단계 수). ⚠️ **마르는 계산이 단계마다 따로 돈다** — 물기가 0에 닿기까지의 봐주기가 그 단계에 한 번씩만 붙어서, 열두 시간을 한꺼번에 미는 것보다 훨씬 빨리 등급이 깎인다. ⚠️ **촉촉비료는 느리게 만든다** (단계 1.5배 · 물 절반). ⚠️ **물뿌리개는 북쪽을 볼 때만 나온다** — 원작이 좌우면 `GF_ASSERT`로 걸린다. **화면에도 선다** — 흙은 `fldeff.narc` 17번 모델이고 그 위에 서는 것은 열매마다의 판때기 193벌이다 (`pnpm shot berry`) |
| 4.7 도구 아이콘·설명 | ✅ | 468종 | |
| 4.8 우편 | ✖ | 쓰기·읽기·보관 | `mail.c`. **뒤로 미뤘다** — 우편함 20통·편지지 12종·낱말 세 문장인데 여는 문이 통신(§9)과 장식뿐이라 아무 길목도 안 막는다 |
| 4.9 **화석 되살리기** | ✅ | 일곱 짝 | 탄갱박물관 안쪽 사람이 받는다 (`engine/world/fossil`). ⚠️ **표의 차례가 목록의 줄 차례다** — 도구 번호순이 아니라 옛호박(103)이 첫 줄이고 뿌리화석(99)이 넷째다. ⚠️ **그 자리에서 포켓몬이 나오지 않는다** — 되살릴 종족을 변수 16564에 적어 두고 화석만 가방에서 뺀다. 실제로 받는 것은 한참 걸은 뒤에 다시 말을 걸 때고, 그 변수가 0이 아니면 새 화석을 안 받는다. ⚠️ **같은 화석 둘은 목록에서 두 줄을 먹는다** — 누적 개수로 세기 때문이다 (`FindFossilAtThreshold`). ⚠️ **화석을 손에 넣을 자리가 지금 없다** — 실측으로 일곱 다 **0곳**이다 (숨은 도구·가게·야생 지님·스크립트 인자 전수, `.audit/probe/literalScan.test.ts`). 원작의 유일한 출처가 지하통로 발굴인데 그쪽이 범위 밖이 되었다(§9). **입수지를 장막백화점 4층으로 옮겼다** (§12.1) — 원작이 안 팔던 것이라 벗어난 결정이고, 그 이유를 §12가 적는다. ⚠️ **캐는 연출은 안 만든다** — 지하통로가 §9로 나가면서 발굴이 미니게임이 아니라 **가게에서 사는 것**이 되었다. 살 수 있으면 캐는 장면이 갈 자리가 없다 |
| 4.10 게임코너 경품 | ✖ | 코인 | §7 |
| 4.11 봉인(seal) | ⊘ | | §9 (콘테스트 계통). **장식은 따로 남겼다** — §7.16 |
| 4.12 **스크립트가 여는 리포트** | ✅ | 공용 스크립트 0x7D6 | 시작 메뉴 말고 **스크립트가 저장을 여는 길**이다 (`CommonScript_SaveGame`). 묻고 답하는 것은 전부 원작 스크립트고(요약창 → "작성할까요?" → 이미 있으면 "덮어써도 괜찮습니까?"), 우리는 밑바닥 여섯만 댄다 — `CheckSaveType` · `OpenSaveInfo`/`CloseSaveInfo` · `TrySaveGame` · `ShowSavingIcon`/`HideSavingIcon`. ⚠️ **「빠른 저장」은 답하지 않는다** — 원작의 그것은 플래시의 바뀐 블록만 쓰는 것인데 우리 리포트는 한 덩이다. 없는 갈래를 답하면 스크립트가 "조금만 저장합니다"라고 거짓말을 한다. ⚠️ **요약창을 메뉴 스택에 안 올린다** — 올리면 필드 입력이 멈춰서 대사창의 「예/아니오」가 키를 못 받는다. ⚠️ **이게 없으면 배틀프런티어에 못 들어간다** (§9.3) — 로비가 도전 앞에서 이 스크립트를 부르고 결과가 0이면 스스로 돌려보낸다. 그래서 팩토리를 만들면서 같이 만들었다 |

## 5. 메뉴·화면

원작 `src/applications/`의 스물셋과 1:1로 놓는다.

| 원작 | 우리 | |
|---|---|---|
| `title_screen` | ✅ | `ui/screens/TitleScreen` |
| `main_menu` (새로하기/이어하기) | ✅ | |
| `naming_screen` | ✅ | `ui/menu/NameScreen` — NDS 터치 자판은 안 옮긴다 |
| `rowan_intro` | ✅ | 오프닝 |
| `choose_starter` | ✅ | 원작 3D 모델 |
| `bag` | ✅ | `ui/menu/BagScreen` |
| `party_menu` | ✅ | `ui/menu/PartyScreen`. Z가 **갈래 메뉴**를 연다 — 차례가 `GetContextMenuEntriesForPartyMon` 그대로다(요약 → 아는 비전기술 → 자리바꾸기 → 지닌물건 → 그만둔다). 알은 요약과 자리바꾸기 둘뿐. 낱말은 전부 파티 뱅크(453)의 것이다 |
| **`pokemon_summary_screen`** | ◐ | `ui/menu/SummaryScreen` — **넷이 뜬다**(정보·트레이너메모·능력·싸울기술). ⚠️ 원작은 여덟 쪽인데 컨디션·콘테스트기술·**리본**은 콘테스트홀에 들어가야 열린다(`TryHideContestPages`) — 그 시설이 §7이라 아직 없다. 알은 메모 쪽 하나뿐(`PokemonSummaryScreen_PageIsVisble`). 메모는 갈래 스물하나와 줄 번호까지 옮겼다(`engine/pokemon/memo.ts`) |
| `pokedex` | ✅ | 신오 210 · 전국 493. 오른쪽이 세 쪽이고(정보·서식지·폼) Tab이 검색을 연다. ⚠️ **정렬 목록을 계산하지 않는다** — 롬에 구워진 마흔일곱 벌을 그대로 쓴다(`zukan_data.narc`). 무게로 다시 정렬하면 동률의 차례가 달라진다. ⚠️ **가나다순 말고는 잡아 본 것만 나온다**(`keepUncaught`). 서식지는 30×30 칸 지도(`zukan_enc_platinum.narc`)고, ⚠️ **들판만 축이 뒤집혀 있다** — 배열이 열 우선인데 던전은 아니다 |
| **`town_map`** | ✅ | `ui/menu/FlyScreen`이 **지도**다. 그림은 롬에서 뽑았고(`tools/extract/townMap.js` — 두 판을 겹쳐야 지도가 된다), 커서가 격자를 한 칸씩 돈다. 날 수 있는 곳 스물은 `sFlyLocations` 그대로이고 **맵으로 찾는다**(`TownMap_GetFlyLocationAtPos`) — 칸으로 견주면 도시 안 세 칸에서 못 난다. 칸을 못 박아 둔 셋(팔파크·챔피언로드·리그)만 예외. Tab이 확대하고, 지금 자리는 깜빡인다. ⚠️ **숨은 자리 넷은 열기 전에 아예 없는 칸이다** — 이름만 가리면 만월도가 「안 가 본 곳」으로 보인다. 여는 것은 매직 넘버다(`sHiddenLocationMagicNumbers`) |
| `trainer_case` (트레이너카드) | ✅ | 뱃지 8 포함 |
| `pc_boxes` | ✅ | 다섯 갈래 다 연다 (`enum BoxMode`) — 맡긴다·꺼낸다·옮긴다·**도구 옮긴다**·**비교한다**. 도구는 빈 칸에 못 놓고 상대가 들고 있으면 맞바꾼다. 비교는 Tab으로 세 쪽(`enum CompareMode`). ⚠️ **콘테스트 쪽은 §3.11이 없어 못 채운다** — 0을 늘어놓지 않고 그렇게 말한다 |
| `options_menu` | ✅ | |
| `move_reminder` (기술 되살리기) | ✅ | 기술가르침과 **같은 화면**을 쓴다 (`FieldSystem_OpenMoveReminderMenu`). 목록은 **지금 레벨까지의 레벨업 기술 중 지금 안 쓰는 것**이다 — ⚠️ 높은 레벨 자리에서 **멈추지 않고 건너뛴다**(표가 오름차순이 아닌 종이 있다). 넷을 다 알면 무엇을 잊을지 묻고, 거기서 물러서면 안 배운다(`keepOldMove`) |
| **`journal_display`** (모험노트) | ✅ | `ui/menu/JournalScreen` — 가방의 열쇠도구에서 연다(`ITEM_USE_FUNC_JOURNAL`). 원작은 아래 화면 한 장이라 우리는 그 종이를 가운데 놓는다. 규칙은 §7.4 |
| **`poketch`** | ✅ | `ui/poketch/` — 앱 **25종**. ⚠️ 화면 배치는 **BDSP**를 따랐다(원 스크린). 규칙은 §7.3 |
| `pc_hall_of_fame` | ✅ | `ui/menu/PCHallOfFameScreen` — 서른 줄을 넘겨 본다. 여섯 마리가 원작의 **손으로 놓은 여섯 자리**에 서고(`spriteCoordinates`) 커서가 앉은 마리가 운다. Z가 「일반」과 「기술」을 오간다. ⚠️ **←가 옛것이고 →가 새것이다** — `LoadLeftEntry`가 「최근에서 몇 번째 거슬러」를 늘린다. ⚠️ ↑는 옛 줄의 **마지막 마리**로, ↓는 새 줄의 **첫 마리**로 간다 |
| `diploma` | ✅ | 신오판·전국판 둘 (`ShowDiplomaSinnoh`·`ShowDiplomaNationalDex`). 네 줄이 다 롬의 글이다 |
| `berry_tag` | ✅ | 열매 64종의 크기·단단함·맛 다섯 (`nuts_data.narc`). ⚠️ **단단함이 1부터 센다** — 0부터로 읽으면 이름 다섯의 마지막이 밖으로 나간다. ⚠️ 원작은 가방의 갈래 메뉴에서 여는데 우리는 아직 그 메뉴가 없어 Tab으로 연다 |
| `poffin_case` | ✖ | §7 콘테스트 |
| `mail` · `mail_viewer` | ✖ | §4.8 — 뒤로 미뤘다 |
| `easy_chat` | ✖ | 낱말 고르기. 무리 14 · 낱말 뱅크 일곱. 먹이는 곳이 우편(§4.8)과 §9뿐이라 같이 미뤘다 |
| `frontier` | ⊘ | §9 |
| `gts_application` | ⊘ | §9 |

## 6. 수집·특수 출현

| | 우리 | 근거 · 다음에 할 일 |
|---|---|---|
| 6.1 도감 보기·잡기 기록 | ✅ | 세이브에 비트필드 둘 |
| 6.2 전국도감 열기 | ✅ | `GetSetNationalDexEnabled`가 켜고 묻는다 |
| 6.3 **배회 포켓몬** (엠라이트·크레세리아) | ✅ | 자리 여섯 · 도로 스물아홉 (`engine/world/roamer.ts`). 세이브에 개체가 통째로 들어 있어서 **도망친 그 마리가 맞은 만큼을 들고** 다른 도로에서 다시 나온다 — 개체값·성격값이 여는 순간 정해지고 만날 때마다 같다. 움직이는 자리가 원작 그대로다: **걸어서 맵을 넘을 때만** 움직이고(16에 1은 신오 아무 데로나, 아니면 옆 도로로 한 칸), 워프로는 안 움직이며, 날거나 순간이동하면 전부 흩어진다. 여기 있어도 절반은 안 나오고, 야생전을 한 판 치를 때마다 30%로 이 맵을 뜬다. ⚠️ **방금 떠난 맵으로는 안 간다** — 그 한 가지를 위해 `PlayerRecentRoutes` 두 칸이 있다. ⚠️ **205번 북쪽의 이웃 표에 208번이 박혀 있다** — 영원시티에 붙은 것은 211번 서쪽인데 원작이 `9`를 적어 두었고, 고치면 도는 모양이 달라지므로 그대로 옮겼다. ⚠️ **묶어 두지 않으면 명령을 내리는 순간 달아난다** (`RoamingPokemon_Main` — 배회에만 물리는 AI가 하는 일은 도망 하나다). 원작에서 도망은 기술이 아니라 도구·교체와 같은 갈래라 우리 기술보다 먼저 처리되므로, 검은눈빛·그림자밟기·개미지옥·조이기 없이는 **한 대도 못 맞힌다.** 볼은 그보다 앞서서 한 판에 한 번 던져 볼 수 있다. 묶였는지는 sim에게 묻는다(`request.active.trapped`) — 원작 목록에 없는 자력만 더 걸리는데 실제로 도는 둘은 강철이 아니다. 자리 여섯 중 넷(다크라이·파이어·썬더·프리져)은 여는 스크립트가 원작에 없다(배포 이벤트용) — 표만 남긴다 |
| 6.4 무리 포켓몬 (스웜) | ✅ | 씨앗이 스물두 곳 중 하나를 고르고, 그 맵의 슬롯 0·1이 `swarm`으로 바뀐다. 여는 것은 신오방송국이다(`EnableSwarms`) — 열기 전에는 아무 데도 안 뜬다 |
| 6.5 **포켓몬레이더** | ✅ | 사슬 · 흔들리는 풀 넷 · 기록 셋 | `engine/world/pokeRadar` · `scene/pokeRadar`. 주인공을 가운데 둔 **9×9 상자**의 고리 넷에서 한 칸씩 뽑는다(바깥부터 32·24·16·8칸, 한 변이 `9 − 고리×2`) — 큰 풀이고 높이와 맵이 같아야 산다. ⚠️ **`TileBehavior_IsTallGrass`는 0x02 하나뿐이다** — 긴 풀(0x03)에는 무더기가 안 선다. ⚠️ **무더기를 밟으면 관문을 안 본다** — 원작이 `gettingEncounter = TRUE`로 덮어써서 반드시 나오고, 그 판에서는 배회도 안 나온다. ⚠️ **첫 판은 사슬을 안 올린다** — 그 판이 종을 정하는 판이다(`unk_14`). 이을 확률은 고리마다 88/68/48/28이고 **잡았으면** 98/78/58/38이다. ⚠️ **끊을 무더기는 흔들림을 다시 던진다**(50/50) — 그래서 「다르게 흔들리면 반드시 끊긴다」는 맞고 「같게 흔들리면 반드시 잇는다」는 틀리다. 강하게 흔들린 자리는 조우표의 **칸 4·5·10·11의 종만** 그 맵의 레이더 넷으로 갈린다(레벨은 원래 칸 것이다). 색 확률은 `8200 − 사슬×200`으로 40번째에 200이 되고, ⚠️ **다시 굴리는 것이 아니라 트레이너 번호에서 성격값을 지어낸다** (`Pokemon_FindShinyPersonality`). 배터리는 오십 걸음이고 **가방에 있을 때만** 찬다. ⚠️ **사슬은 리포트에 안 남는다** — 원작도 배터리와 기록 셋만 적는다. ⚠️ **흔드는 그림은 우리 것이다** — 원작은 오버월드 애니 갈래 셋(`ov5_021F3154`의 0·1·2)을 얹는데 우리는 안 굽는다. **세기 셋과 그 뜻**(약하게·강하게·색이 다르게)은 그대로다 |
| 6.6 **꿀 나무** | ✅ | 21그루 · 무리 넷 · 먹이몬 나무 | `engine/world/honeyTree.ts`. 나무 앞에서 **북쪽을 볼 때만** 열린다(`HoneyTree_TryInteract`) — 스물한 그루에 BG 이벤트가 하나도 안 붙어 있어서 배치표의 모델 번호(`honey_tree.nsbmd` = 26)만이 나무를 가린다. 실측으로 스물한 맵에 한 그루씩 있고 그 밖에는 하나도 없다. 바르면 하루치(1440분)가 차고 **여섯 시간이 지나야**(남은 분 ≤ 1080) 붙는다. ⚠️ **켜 둔 동안만 세지 않는다** — 마지막으로 본 분을 리포트에 두고 그 차이만큼 깎는다(`daily.minute` · `inline_020559DC`). 시계를 뒤로 돌리면 0이다. ⚠️ **먹이몬 나무 넷은 트레이너 번호가 정한다** — 32비트를 네 바이트로 잘라 21로 나눈 나머지고, 겹치면 밀어서 늘 서로 다른 넷이다. 보이는 다섯 자리만 쓰면 둘이 0·1번으로 굳는다. ⚠️ **같은 나무에 연달아 바르면 90%로 무리가 그대로다**(자리와 흔들림만 다시 뽑는다) — 이게 좋은 무리를 이어 가는 길이다. 흔들림 분포도 실측이다(무리 C가 세 번 흔들 확률 93%지만 없음·A도 1%, B는 4%다 — 「세 번이면 먹이몬」이 아니다). 레벨은 5~15고 선두가 의욕·의기양양·프레셔면 절반의 확률로 15다. ⚠️ **흔드는 그림은 우리 것이다** — 원작은 소품 모델에 딸린 nsbca 세 벌을 얹는데(`MapPropAnimationManager`) 우리 소품 파이프라인은 정지 메시만 굽는다. **세기 셋**(`GetShakingValue`의 0·1·2)은 그대로 옮기고 흔드는 모양만 `scene/honeyTree`가 만든다. ⚠️ **가방에서 꿀을 쓰는 길은 아직 없다** — 원작의 그쪽은 나무와 무관한 「달콤한향기」다(`UseHoneyFromMenu`가 지금 선 칸에 야생을 부른다) |
| 6.7 트로피가든 | ✅ | 슬롯 6·7이 특별 16종으로 바뀐다. 하루 한 마리씩 더하고(`AddTrophyGardenMon` — **겹치면 다시 굴린다**) 새 것이 앞자리로 온다. ⚠️ 전국도감 전에는 안 바뀐다 |
| 6.8 **안농 28폼** | ✅ | 표 여덟 · 본 글자 기록 | 글자를 정하는 표 여덟은 §3.4가 이미 돌리고 있었다. 남아 있던 것은 **본 글자를 적는 자리**다 (`engine/pokemon/dex`의 `seeUnownForm`). ⚠️ **비트필드가 아니라 「본 차례대로의 목록」이다** — 원작이 배열 앞에서부터 채우고 0xFF로 끝을 표시해서, 도감의 폼 쪽이 처음 본 차례로 늘어선다. ⚠️ **잡을 필요가 없다** — 야생으로 무대에 서는 순간 적힌다(`BattleSystem_DexFlagSeen`과 같은 자리). 스물여섯을 다 봐야 매니아터널의 비밀방이 열리고, 「!」와 「?」는 거기서만 나온다 (표 7). 리포트 판 24 |
| 6.9 화석 | ✅ | §4.9. ⚠️ 캐 오는 자리(지하통로 §7.2)는 아직 없다 — 스토리로 받는 두개·갑옷화석과 지하통로 밖에서 줍는 것만 된다 |
| 6.10 전설 (기라티나·아르세우스·레지 3) | ✅ | **깨어진 세계를 끝까지 내려간다.** 떠 있는 판 넷 갈래의 통행·벽 중력·뛰는 자리·사건이 돌고(`engine/world/distortion.ts` · `scene/distortion.ts`), **승강 발판이 층을 잇는다**(`distortionElevator.ts`) — 발판 열여덟 · 경로 22 · 자리 표 열하나. 자료는 `tw_arc`·`tw_arc_attr`에서 나온다(맵 10 · 판 10 · 뛰는 자리 20 · 통행 격자 12벌 · 사건 45자리). 바위 수수께끼도 돈다(`distortionBoulder.ts`) — 떨어지는 자리 열넷 · 바위 자리 열일곱. 오리진폼 기라티나전, 「운명적인 만남」 조우, 귀혼동굴의 방 굴리기도 붙었다. ⚠️ **경로 표가 층의 높이 차와 맞는지 실측으로 못 박았다** — 승강 발판 열여덟을 다 태워 보고 `finalTileYOffset` 합이 `tw_arc`의 층 오프셋 차와 같은지 본다. 두 층을 한 번에 지나는 자리 둘(B3F↔B5F)도 그 안에 든다. ⚠️ **아무도 안 쓰는 경로 둘(20·21)은 원작이 잘못 적어 두었다** — 걸음의 부호가 목표와 반대라 태우면 영영 안 멈춘다. 표를 고치지 않고 길이를 재서 막는다. ⚠️ **틀린 웅덩이에 빠진 바위는 B5F 자리로 되돌아간다** — 사라지는 것이 아니다. 남은 전설 넷(레지기가스·아르세우스·다크라이·크레세리아)도 스크립트가 한 자리도 안 비고 돈다(`vm.test.ts`의 「이야기를 끝내는 맵들」). 실제로 만나려면 원작과 같은 것이 필요하다 — 레지 3 소지 · 배포 이벤트(하늘의 피리 · 멤버즈카드). **소품 스물다섯이 원작 모델로 선다**(`/data/mmodel/fldeff.narc` 0x7C~0x94, 삼각형 3,390) — 밟으면 나타나는 블록이 네 층에 42개다(576:23 · 577:5 · 582:6 · 583:8). ⚠️ **소품은 세 갈래를 다 세워야 한다** — 유령 소품 212 · 승강 발판 34 · 늘 서 있는 문·폭포·덩굴 4로 열 층에 250개다(`InitGhostPropManager` · `InitMovingPlatformPropsForMap` · `InitSimplePropsForMap`). 유령 소품만 그리면 **타야 할 승강 발판이 통째로 안 보인다** — 1F에서 아래로 내려가는 그 판이 그것이다. ⚠️ **자리 보정이 있다**(`sPropInitialPosOffsetByKind`) — 원작이 칸 한가운데(+0.5)에 이 값을 더한다. 작은 발판은 y가 −25/16이라 모델 윗면이 「선 높이 − 1/16」에 오고, 빼먹으면 발판이 한 칸 넘게 떠서 사람이 그 속을 걷는다. ⚠️ **두 칸을 건너뛰는 자리가 224칸 있다**(`TILE_BEHAVIOR_JUMP_*_TWICE` 0x5A~0x5D) — 이 세계에만 있고 다른 맵에는 하나도 없다. 걷는 칸 5,086개가 112군데에서 두 칸씩 끊겨 있어서, 이 뜀이 없으면 세계가 통째로 끊긴 섬이 된다 — 1F의 승강 발판 자리(지역 x 17~21 · z 42~46)는 사방이 막혀 있어 뛰지 않고는 못 들어간다. **세 칸**을 간다(원작이 프레임마다 2/16타일씩 24프레임, `MovementAction_JumpDistortionWorldNorth`) — 224칸 전부에서 출발 한 칸 뒤와 착지 세 칸 앞이 걸을 수 있는 자리라는 것이 그 거리를 못 박는다. ⚠️ **유령 소품 방아쇠·카메라 각·뛰는 자리는 떠나는 칸에서 돈다**(`DistWorld_HandlePlayerMoved`) — 도착한 칸에서 돌리면 「방아쇠 칸에 서서 돌아선 뒤 걷기」가 빠져 블록이 안 나타난다. 승강 발판·사건·스크립트 칸만 닿은 칸에서 본다(`..._HandlePlayerPositionChanged`). ⚠️ **처음 상태는 `~defaultVisiblePropGroups`다** — 0으로 두면 스물네 무리가 다 보여서 나타날 블록이 이미 서 있다. 층을 갈아탈 때마다 그 층 기본값으로 **다시 세운다**. ⚠️ **기라티나 방 발판은 방아쇠가 아니라 사건이 세운다** — 무리 1·2·3을 48프레임 간격으로. 이 명령 둘을 「연출」로 넘기면 그 방에서 못 나간다. ⚠️ **맵 581의 유일한 방아쇠는 소품이 없는 무리 0을 가리킨다** — 원작이 그렇다. 밟아도 아무 일도 안 일어난다. ⚠️ **여기서는 지면을 안 따라간다** — 원작이 이 세계에 들어서면서 주인공의 높이 계산을 꺼 버리고(`InitPlayer`의 `MapObject_SetHeightCalculationDisabled(TRUE)`), 처음 서는 높이를 `LoadFloor`가 `mapOffset.y + MAP_OBJECT_TILE_SIZE` 한 줄로 적는다. 즉 **지역 y가 한 층 내내 1**이고, 승강 경로의 y 변화(−32·−14·−50)가 층 오프셋 차와 같아서 열 층 내내 보존된다. 판의 bounds나 배치표 중앙값으로 짐작하면 **B2F에서 여덟 칸이 뜬다** — 그 층의 바닥 판 넷은 지역 9인데 그건 서쪽 벽을 타고 올라가야 닿는 위층이고, 걷는 바닥은 지역 1이다(실측: 걸을 수 있는 196칸 중 170칸의 그림이 지역 1). ⚠️ **층에 들어서면 그 층의 사람과 바위를 세운다**(`AddMapObjectsForMap`) — 이게 없으면 기라티나도 호수 셋도 바위 아홉도 한 번도 안 나타난다. `manualAddOnly`는 스크립트가 부를 때만 서고, 나머지는 `CheckFlagCondition`과 숨김 플래그를 본다. 다음 층 것은 안 세운다 — 우리 배우는 층마다 지역 좌표라 다음 층 사람이 이 층 한복판에 선다. ⚠️ **그 사람들의 그림은 배치표에 없다** — `events.json`만 훑던 추출기가 기라티나(230)·엠라이트(264)·B6F의 셋(265~267)을 빼먹어서, 세워도 아무것도 안 그려졌다. ⚠️ **승강 발판을 같이 타는 사람은 닿는 층 표에 없다** — 원작이 새로 세우지 않고 타고 온 객체의 번호만 갈아 끼운다(`MapObject_SetLocalID`). 닿는 층에서 찾으면 B1F의 시로나가 대사만 남고 사라진다. ⚠️ **벽으로 건너뛰는 자리는 열여섯 프레임짜리다**(`movementAnimSteps`) — 한 프레임에 옮기고 판을 바로 갈아 끼우면 중력 축이 그 프레임에 뒤집혀 화면이 뚝 끊긴다. 원작은 다 옮긴 뒤에 판을 갈고, 카메라도 그 열여섯 프레임에 걸쳐 돈다 |
| 6.11 **날짜가 바뀌는 것** (일일 씨앗) | ✅ | 이 하나가 6.4·6.7과 빈티나(§1.5)·대습초원(§7.7)을 같이 연다. `engine/world/daily.ts`. 새 게임에서 한 번 뽑고 **하루에 한 번** `ARNG_Next(x) = x·1812433253 + 1`로 굴린다 — 32비트 곱이라 `Math.imul`이어야 한다. 시계를 뒤로 돌려도 안 굴린다. 자료는 `/arc/encdata_ex.narc` 12칸(`encountersEx.json`): 0 빈티나 종족(349) · 1 천관산 B1F 낚시 칸 528개 · 2~7 꿀나무 · 8 트로피가든 16종 · 9~11 대습초원. **낚시 칸 528개가 전부 물 위**라는 것을 타일 격자로 교차검증했다 |
| 6.12 **특별한 날** | ✅ | 마흔세 날이 걸음마다의 평평한 관문을 ±5·±10 움직인다 (`engine/world/specialDates.ts`). 관문의 바닥이 40이라 −10은 4분의 1을 깎는 것이다. `sJanuaryDays`…`sDecemberDays` 열두 배열 그대로고, **보정 0인 날 넷**(2/14 · 5/1 · 5/4 · 11/23)도 지우지 않고 남겼다 |

## 7. 부가 시설

모험노트(§7.4) 말고는 없다. 원작 파일과 우리가 잰 스크립트 자리 수를 같이 적는다.

| | 원작 | 자리 |
|---|---|---|
| 7.1 콘테스트 (+ 포핀 · 씰 · 사진 · 배경) ⊘ | `contest.c`, `poffin.c`, `applications/poffin_case`, `overlay022` | 199 — §9. **장식과 리본은 뺐다**(§7.16 · §3.10) |
| 7.2 지하통로 (비밀기지 · 발굴 · 구슬 · 함정) ⊘ | `src/underground/` 20파일 | 25 — §9. 계통표의 52는 **다른 것 셋이 섞인 수**다 (§9.1) |
| 7.3 포켓치 ✅ | `applications/poketch/` 25앱 | 61 |
| 7.4 모험노트 ✅ | `journal.c` | 156 |
| 7.5 게임 기록 · 랭킹 · TV | `game_records.c`, `rankings.c`, `tv_segment.c` | 위 156에 포함 |
| 7.6 게임코너 · 복권 | `scrcmd_game_corner_prize.c`, `scrcmd_jubilife_lottery.c` | 12 — 소지금·코인 창과 코인 셈은 됐고(§7.15) 상품표·복권만 남았다 |
| 7.7 대습초원 사파리 ◐ | `catching_show.c`, `great_marsh_lookout.c` | **필드 쪽은 됐다** — 볼 서른·걸음 오백·습초원 열차 (`world/safari`). 남은 것은 **사파리 배틀**(볼·먹이·진흙·도망 넷과 도망 판정)뿐이다. 일일 출현표는 §6.11에서 이미 읽는다. ⚠️ **전망대 둘은 안 만든다** — 대습초원 전망대도 길잡이등대 쌍안경도 카메라를 옮겨 경치를 보여 주는 것이 전부고 규칙도 진행도 안 건드린다. 명령은 만들어 **아무 일도 안 하게** 뒀다 — 「아직 못 만든 것」과 「안 만들기로 한 것」이 계통표에서 같은 줄로 보이면 안 된다 |
| 7.8 상호교류광장 ✅ | `scrcmd_amity_square.c` | 명령 여섯이 다 돈다. 따라다니는 마리가 장식을 주워 온다 — **무엇을 주워 오는지는 데려간 마리가 정한다**(무리 여섯, 표에 없는 종은 0번). ⚠️ **걸음을 광장 밖에서도 센다** — 원작이 어느 맵에서든 올리고 광장에 들어설 때 스크립트가 0으로 지운다. 표 둘 다 코드 안에 있어 디컴프에서 굽는다 (`pnpm gen:amity`). 주워 온 장식은 케이스로 들어간다 (§7.16) — 그 길을 실제 스크립트로 밟는 것이 `script/amitySquare.test.ts`다 |
| 7.9 승부기록기 · VS시커 | `unk_*`, `UseVsSeekerInField` | — |
| 7.10 배틀타워 · 프론티어 ◐ | `battle_frontier.c` 외 | 185 — 배틀팩토리 하나가 돈다 (§9.3). 나머지 넷은 §9 |
| 7.11 명예의 전당 ✅ | `cutscenes/hall_of_fame.c`, `clear_game.c`, `hall_of_fame_entries.c`, `applications/pc_hall_of_fame/` | — |
| 7.12 엘리베이터 · 승강판 · 체육관 장치 ✅ | `platform_lift.c` · `overlay008/gym_features.c` | 88 — §1.23으로 옮겼다. **부가 시설이 아니라 길이었다** |
| 7.13 크기 대회 ✅ | `overlay005/size_contest.c` | 222번도로 동쪽 집. 총어 전용이다 |
| 7.14 트레이너의 「모습」 ✅ | `appearance.c` | 무쇠시티 포켓몬센터. 후보 넷이 트레이너 번호로 갈린다 |
| 7.15 소지금·코인 창 ✅ | `overlay005/field_menu.c` | 상점·육성가·게임코너 앞의 작은 창. 자리는 타일 좌표 그대로다 |
| 7.16 장식 케이스 (패션 케이스) ✅ | `unk_020298BC.c` | 콘테스트에서 **떼어 냈다**(§9.1). 장식 100가지를 담는다 — **겹치는 61가지는 4비트씩 아홉까지, 하나뿐인 39가지는 1비트씩**이라 리포트에 열 칸으로 들어간다 (`world/fashionCase`). ⚠️ **읽을 때도 아홉으로 깎는다** — 4비트가 15까지 담기므로 안 깎으면 망가진 리포트가 열 개를 낸다. ⚠️ **여는 화면은 없다** — 옷 갈아입히기는 콘테스트 쪽이라 모으는 것까지다. 이름은 롬 뱅크 386의 100줄이고 **61번(컬러풀파라솔)이 경계**다 |

⚠️ **7.15는 값을 실시간으로 안 읽는다.** 원작이 창을 만들 때와 `Update…`를
부를 때만 숫자를 다시 찍는다 — 돈을 깎고 나서 갱신 명령 전까지는 옛 숫자가
그대로 떠 있는 것이 원작의 모습이라 그 순간의 값을 붙잡아 둔다.

⚠️ **7.13의 키는 도감 표가 아니라 종족 자료의 `heightDm`을 쓴다.** 원작은
`pokedex_heightweight`의 narc를 읽는데, 둘을 493종 전부 맞대 보니 **기라티나
하나만 다르다**(종족 자료 4.5m / 도감 글 6.9m — 어나더와 오리진). 대회가 총어
전용이라 그 차이가 답에 닿지 않는다 (`engine/world/sizeContest.test.ts`).

### 7.3 포켓치 — 앱 스물다섯과 그 배치

규칙은 `engine/world/poketch.ts`, 화면은 `ui/poketch/`, 세계와 잇는 것은
`scene/poketch.ts`다. 앱 이름 25개와 기술효과체커의 일곱 줄은 롬 뱅크에서 온다
(`poketch_app_names` 457 · `poketch_move_tester` 456).

⚠️ **화면 배치만 BDSP를 따랐다.** 플래티넘의 포켓치는 **아래 화면을 통째로
쓰는 것이 전제**고 우리는 원 스크린이다. 같은 게임을 원 스크린으로 다시 만든
것이 BDSP이고, 거기서는 오른쪽 위 구석의 작은 시계로 놓인다 — R로 키우고 접고,
길게 누르면 감춘다. **앱 스물다섯과 그 규칙은 플래티넘 것 그대로다** (BDSP는
나무열매탐색기·기술효과체커·스톱워치·알람시계를 빼고 「비전기술」을 더했다 —
우리는 플래티넘을 만들고 있으므로 그쪽을 따르지 않는다).

⚠️ **앱이 쓰던 값은 리포트에 안 남고, 앱을 넘기면 사라진다.** 원작
`PoketchMemory`가 12KB 버퍼 **하나**를 스물다섯이 돌려 쓰고, 되읽을 때
`appID == activeAppID`를 본다 (`poketch_memory.c`). 계산기에 숫자를 남기고
만보기에 갔다 오면 계산기가 0으로 돌아온다. 리포트에 남는 것은 등록된 앱 ·
화면 색 · 걸음 · 알람 · 캘린더 표시 · 마킹맵 표식 · 포켓몬이력 · **도트아트**뿐이다.

| 상태 | 우리 | 근거 |
|---|---|---|
| 디지털시계 · 아날로그시계 | ✅ | 게임 시계를 본다 |
| 계산기 | ✅ | 4×4 자판. ⚠️ **0으로 나누면 0**이다 |
| 메모용지 · 도트아트 · 룰렛 | ✅ | 24×20 격자. 도트아트만 밝기 넷이고 **리포트에 남는다** |
| 만보기 | ✅ | ⚠️ **받기 전 걸음은 소급 안 된다** (`Poketch_SetStepCount`가 앞에서 막는다) |
| 포켓몬리스트 · 친밀도체커 · 상성체커 | ✅ | 하트 갈래는 1·35·70·150·200·255 |
| 키우미집체커 · 포켓몬히스토리 | ✅ | 이력은 열둘이 차면 앞이 밀린다 |
| 카운터 · 동전던지기 · 스톱워치 · 키친타이머 · 알람시계 | ✅ | |
| 캘린더 | ✅ | ⚠️ **달이 바뀌면 앞 표시가 통째로 사라진다** — 표시가 32비트 하나뿐이다 |
| 컬러체인저 | ✅ | 여덟 색이 액정 두 색을 갈아 끼운다 |
| 마킹맵 | ◐ | 표식 여섯이 놓이고 지금 자리가 보인다. ⚠️ 원작은 터치로 끌지만 우리는 「여기에 꽂는다」 |
| 기술효과체커 | ✅ | ⚠️ **곱셈이 아니라 덧셈이다** — 3에서 시작해 2배 +1, 0.5배 −1. 그래서 4배(5)와 2배(4)가 갈린다 |
| 다우징머신 | ◐ | 앞뒤 열다섯 칸 안의 안 주운 숨은 도구. 탐지 반경 0·1·2를 점 크기로 남긴다(`hiddenItems.json` 257개). ⚠️ 원작은 **터치한 자리** 둘레를 훑는데 우리는 손가락이 없어 창을 통째로 보여 준다 |
| 나무열매탐색기 | ✖ | 밭은 돈다 (§4.6). 앱 화면이 아직 없다 |
| 통신탐색기 | ⊘ | §9 |
| 포켓트레카운터 | ✖ | VS시커가 없다 (§7.9). ⚠️ 사슬 기록 셋은 리포트에 이미 있다 (§6.5) — 이 앱이 그것도 같이 보여 준다 |

⚠️ **크게 펼치면 필드 입력이 멈춘다.** 원작은 손가락으로 아래 화면을 만지며
걸을 수 있었지만 우리는 키보드가 하나다. 작게 접힌 채로는 시계도 걸음도 파티
체력도 살아서 도니, 걸으면서 보는 쪽은 그대로 남는다.

⚠️ **기계를 켜는 명령이 원작에 없다.** `Poketch_Enable`을 부르는 것은 축복시티의
포켓치 화면(필드 과제)이고 스크립트 명령이 아니다. 우리는 **앱을 처음 등록할 때**
같이 켠다 — 사장이 기계를 주면서 앱도 등록하므로 같은 순간이다.

### 7.4 모험노트 — 무엇이 어떻게 적히는가

규칙은 `engine/world/journal.ts`, 적는 자리는 `scene/journal.ts`, 글로 바꾸는
것은 `ui/menu/journalLines.ts`다. 문장 103개는 `journal_entries` 뱅크(366)에서
그대로 나오고, 빈칸에 넣는 것도 지역명·도구명·종족명·체육관명(378)·시간대(608)
표다 — 우리가 지은 글자가 없다.

**한 쪽의 칸이 정해져 있다.** 머리글(날짜·요일·그날 처음 선 자리) · 자리 일
**넷** · 포켓몬 **한 줄** · 트레이너 **한 줄** · 통신 **둘**. 열 쪽이 남고
날이 바뀌면 한 칸씩 밀린다(`Journal_GetSavedPage`). 자리 일만 쌓이고 나머지는
덮어쓰므로, 하루에 백 마리를 잡아도 남는 것은 마지막 한 마리다.

**겹침을 거르는 방식이 갈래마다 다르다** — 원작 함수 일곱을 그대로 옮겼다:

| 갈래 | 거르는 법 |
|---|---|
| 곁값 없는 일 (박스·상점·게임코너…) | 바로 앞이 같은 갈래면 안 적는다 |
| 자리·도구·비전기술 | 바로 앞이 같은 갈래 **+ 같은 곁값**이면 안 적는다 |
| 체육관 | 같은 체육관의 「강적뿐이다」를 **지우고** 적는다 |
| 사천왕 | 앞선 사천왕 줄을 **지우고** 적는다 — 넷을 다 이겨도 한 줄이다 |
| 챔피언 | 안 거른다 |

⚠️ **여기서 놓치기 쉬운 것 넷:**

- **쓰러뜨린 야생은 다섯째부터 적힌다** (`wildMonDefeated >= 5`). 그리고 그
  계수기는 **맵을 갈아 끼울 때마다 0으로 돌아간다** — 리포트에 안 남는다.
- **말투 셋을 플레이 시간의 분이 정한다.** `분 / 10`이 0·2·4면 「…을 …에
  잡았다!」, 1·3이면 「…에 …을 잡았다!」, 5면 성별 문장이다. 무작위가 아니다.
- **보통 트레이너 줄은 글자 수로 문장을 고른다.** 지역명 + 트레이너 이름이
  14 · 16 · 19자를 넘을 때마다 짧은 문장으로 바뀐다 — 원작이 한 줄에 안 들어가는
  것을 그렇게 막았다.
- **처음 온 마을이면 「나왔다」가 아니라 「도착!」이다.** 원작이
  `if (CreateFlyLocationJournalEvent(...) == FALSE)`로 갈라서 둘 중 하나만 적힌다.

⚠️ **적히는 건물이 스물일곱뿐이다** (`sMapsInfo`). 포켓몬센터를 드나든 것까지
다 적으면 넉 줄이 그것으로 찬다. 그중 「나왔다」로 적히는 것과 「떠났다」로
적히는 것이 표에 따로 있고, **굴로 적혀 있지만 「떠났다」를 쓰는 셋**
(골짜기발전소·선단신전·골풀무제철소)도 원작이 손으로 골라 냈다.

⚠️ **상점은 금액이 아니라 횟수로 가른다.** 산 횟수가 둘 이상이면 「많이 샀다」다 —
백만 원짜리를 한 번 사도 그냥 「쇼핑했다」다. 장막백화점·영원시티 허브샵·
봉신마을 북서쪽 집 셋은 아예 안 적는다.

**아직 없는 것**: 통신 칸 둘에 들어가는 일 대부분이 §9(통신)다. 지금 날 수 있는
것은 콘테스트 등수와 포핀뿐이고 그 둘은 §7.1이 붙을 때 이어진다.

## 8. 연출·소리

| | 우리 | |
|---|---|---|
| 8.1 BGM · 효과음 · 울음소리 493 | ✅ | SSEQ를 직접 렌더 |
| 8.2 낮/밤 24칸 표 | ✅ | |
| 8.3 날씨 | ◐ | 맵 헤더의 날씨 번호로 비·눈·모래바람·안개·번개가 실제로 내린다 (`scene/FieldWeather`). ⚠️ 스크립트가 날씨를 바꾸는 길이 없다 — `GetOverworldWeather` 계통 13자리 |
| 8.4 화면 페이드 | ✅ | |
| 8.5 문 여닫는 애니 | ◐ | 문짝이 실제로 돈다 (`scene/DoorAnimations`, 200ms). ⚠️ **원작 NSBCA가 아니라 우리가 돌리는 것이다** — 소품 애니를 아직 안 굽는다 |
| 8.6 관절 애니 (JNT0) | ◐ | 길이는 헤더에서 읽어 박자를 맞추고, 도중 움직임이 없다 |
| 8.7 포켓몬 3D 493 | ✅ | 폼 64개까지 557벌 (§3.4) |
| 8.8 인물 3D | ◐ | GLB 95벌이 배치의 **42.4%**(3,555 중 1,508)에 선다. 남은 이들의 BDSP 이름표는 이미 있다 ([3D_GAP_AUDIT](3D_GAP_AUDIT.md) §3.1) |
| 8.9 진화 연출 | ✅ | 배틀이 끝나면 열린다 (`ui/menu/EvolutionScreen`). B로 멈출 수 있는 자리까지 §3.1과 한 몸 |
| 8.10 알 부화 연출 | ✅ | 화면이 흔들리고 깨는 것까지 돈다 (`ui/menu/HatchScreen`, 1400ms). 알 그림은 `pl_otherpoke`에서 온다 — `pl_pokegra`에 칸이 없어서 한동안 아이콘을 키워 썼다 (§3.4). 흔들리는 동안에는 여전히 아이콘이다 |
| 8.11 명예의 전당 연출 | ◐ | `ui/menu/HallOfFameScreen` — 원작 상태 다섯을 그대로 밟는다(밝아진다 → 한 마리씩 → 파티와 주인공 → 검게 닫힌다 → 어두워진다). 자리와 박자가 원작 픽셀·프레임 그대로다 — 판 96×128이 24/136에서 미끄러져 오고(28프레임), 글 일곱 줄이 136픽셀 칸 안에서 20프레임 간격으로 뜨고, 주인공 판이 88~168에서 0~255로 벌어진다. 끝나면 리포트를 쓰고 **타이틀로 나간다**(`OS_ResetSystem`). ⚠️ **크레딧(스태프롤)이 없다** — `graphic/ending.narc` 85칸을 아직 안 굽는다(오버레이 99). ⚠️ **배경 타일맵도 없다**(`dendou_demo`) — 조명 여섯만 원작 각도로 돈다. ⚠️ **회복기에 볼이 놓이는 연출이 없다** — 소품 하나를 자리 잡아 새로 띄울 길이 아직 없어 소리만 난다(`PlayPokecenterHealingAnimation`도 같다) |

## 9. 범위 밖 — 이유를 적는다

안 하는 것도 결정이므로 적어 둔다. "못 한다"가 아니라 **"이 판에서는 뜻이
없다"**는 것이다.

| | 왜 |
|---|---|
| 통신 대전·교환 · 유니온룸 · GTS · Wi-Fi 광장 | 상대가 있어야 성립한다. 서버를 세우는 일이고 게임 이식이 아니다. 스크립트 자리 158 |
| 미스터리기프트(배포) | 위와 같다. 배포 서버가 닫힌 지 오래다 |
| 배틀타워·프론티어 | 통신 상대·기록 교환이 절반이고, 나머지 절반(연속 대전)은 §2 위에 얹으면 되므로 **§2가 끝난 뒤에 다시 본다** |
| **콘테스트 계통** (§7.1) | 대회·포핀·씰·사진·배경. 만드는 시간에 비해 여는 것이 적다 — 아래 §9.1이 무엇을 잃는지 잰다. **장식(§7.16)과 리본(§3.10)은 뺀다** |
| **지하통로** (§7.2) | 비밀기지·발굴·구슬·함정. 위와 같다 |
| **리본** (§3.10) | 개체마다 리본 칸을 내고 요약 화면에 쪽을 붙여야 하는데, 그 리본을 주는 곳이 대부분 콘테스트다. **리본신드롬도 같이 닫힌다** (§9.2) |
| NDS 터치 자판 | 입력 장치가 다르다. 키보드 자판으로 대신한다 |
| 모바일 | 노트북 이상이 목표다 (PLAN §1.2) |

### 9.1 콘테스트·지하통로를 접으면서 잰 것

접는 것도 **실측을 남긴다**. 아래는 전부 다시 잴 수 있다 (§11).

**⚠️ 계통표(§10)의 이름표가 엉뚱한 것을 끌어왔다.** 정규식이라 이름만 보고
묶어서, 「지하통로 52자리」에 지하통로가 아닌 것이 **셋** 섞여 있었다. 명령
이름까지 세면:

| 지하통로 52자리의 실체 | 자리 · 종 | |
|---|---|---|
| 지하통로 본체 | 25 · 11 | 굿즈 PC · 구슬 · 함정 · 발굴 기록 |
| UG 맵 인사말 | 17 · 2 | `UndergroundNPCMessage` — **통신 놀이용**이라 원래 §9다 |
| 별장 가구 | 8 · 2 | 리조트에리어 별장이다 (`villa_furniture.c`). ⚠️ **조건이 배틀프런티어 기록**이라 이것도 §9다 |
| 백화점 4층 장식품 | 2 · 1 | `PokeMartDecor` — §7.6에 이미 있다 |

**⚠️ 콘테스트 199자리 중 대회 본체는 68자리(34%)뿐이다.** 나머지는 딸린 계통
여섯이고, 그중 둘은 콘테스트 없이도 뜻이 있어 **뺐다**:

| | 자리 · 종 | |
|---|---|---|
| 대회 본체 | 68 · 23 | 접는다 |
| 리본 | 38 · 6 | 접는다 — §9.2 |
| 사진 (드레스업·콘테스트) | 28 · 4 | 접는다 |
| 씰 | 27 · 7 | 접는다 |
| 장식 | 21 · 7 | **남겼다** — §7.16이 돈다 |
| 배경 | 10 · 3 | 접는다 |
| 포핀 | 7 · 4 | 접는다 |

**⚠️ 문 하나 잠그는 일이 아니다.** 콘테스트 계통은 **맵 16곳 · 스크립트 파일
21개**에 흩어져 있다 — 축복시티 · 축복TV방송국 · 신수마을 · 물가시티 ·
들판시티 · 장막백화점 · 영원의 숲 · 206번도로 · 팔파크 · 글로벌 터미널 ·
상호교류광장 · 발자국박사의 집 · 리조트에리어. 콘테스트회장 자체는 8자리다.
**그래서 코드로 문을 잠그지 않는다** — 원작에 없는 문을 만드는 일이고, 흩어진
NPC를 하나씩 막는 것도 원작 훼손이다. 안 만든 명령은 인자 폭만큼 건너뛰므로
(`buildCommands`의 `skipper`) 스크립트는 서지 않는다.

**무엇을 못 잡게 되는가.**

| 접는 것 | 못 잡는 종 | 신오도감 안 |
|---|---|---|
| 지하통로 | 화석 7계열 **14종** | 4 (두개도스·램펄드·방패톱스·바리톱스) |
| 콘테스트 | 밀로틱 | 1 |

- ✅ **전국도감 해금은 안 막힌다.** 신오도감 완성 조건이 「잡은 수」가 아니라
  **「본 수」**다 (`Pokedex_LocalDexCompleted`가 `numSeen >= LOCAL_DEX_GOAL`).
  그리고 다 볼 수 있다 — 두개도스는 관장 246, 바리톱스는 관장 250, 밀로틱은
  챔피언 267이 쓴다.
- ⚠️ 밀로틱과 열두 교환 진화는 **§12의 도구 둘**이 도로 연다.
- ⚠️ **조각 넷의 밭이 사라진다.** 온 게임에 4~5곳뿐이고(숨은 도구 3~4 +
  골풀무제철소 1) 숨은 도구는 플래그로 잠기는 **일회성**이다. 원작의 밭이
  지하통로다 — §10의 「기술가르침 17자리·7종」을 만들 때 **같이 정해야 한다**.
- ⚠️ **플레이트 일곱은 필드 출처가 없다** (물방울·고드름·주먹·대지·이상한·
  원령·공포). 아홉만 숨은 도구로 나온다. 아르세우스 계통이 §9라 지금은 뜻이
  없지만 적어 둔다.

**아끼는 크기.** 자리로는 224/1,300(17%)인데 **자리 수는 화면 수를 못 잰다.**
디컴프 줄 수로 재면 지하통로 **23,521줄**(이름이 다 붙어 있다 — `mining.c`
3,179 · `traps.c` 5,107 · `secret_bases.c` 2,774), 콘테스트 계통 **≈14,000줄**.
⚠️ **그중 대회 앱 `overlay022` 7,398줄은 파일 17개가 전부 `ov22_0225XXXX` 꼴로
함수 이름이 하나도 안 붙어 있다** — 디컴프가 도움을 안 준다. 견줌: 포켓치
21,865줄 · 배틀 50,910줄(둘 다 만들었다). **지하통로 하나가 포켓치보다 크다.**

**되돌리기 비용은 낮다.** 리포트 스키마에 콘테스트 능력치 칸이 아직 없어서
지금 접으면 마이그레이션이 안 생기고, 나중에 되살려도 그때 하나 더하면 된다.

### 9.2 리본신드롬은 통째로 닫는다

리본을 접으면 그 건물이 빈다. **문만 죽는 것이 아니라 내용이 다 리본이다** —
실측:

| 층 | 무엇이 있나 |
|---|---|
| 1F | 입장 판정 `CountPartyRibbons ≥ 10`. 못 넘으면 안내원이 막고 **화면이 어두워지며 쫓겨난다** |
| 1F | **리본 가게다.** 고급리본 10,000 · 로열리본 100,000 · 고급로열리본 999,999원을 받고 `SetPartyMonRibbon`으로 파티 첫 마리에 붙인다 |
| 2F | 날마다 한 번 마사지 (`IncreasePartyMonFriendship` ×3) |

⚠️ **가게로 바꿀 수가 없다.** 그 건물 스크립트에는 `PokeMart`가 한 줄도 없다 —
돈을 받고 리본을 붙이는 제 스크립트를 쓴다. 도구 가게로 만들려면 **롬에 없는
대사를 지어내야** 해서 안 한다.

⚠️ **2층 마사지도 같이 닫는다.** 친밀도를 올리는 자리라 친밀도 진화와 이어져
있지만, 원작에서 그 방에 드는 길이 리본뿐이다 — 문 조건만 바꿔 살리면 원작에
없는 문을 만드는 것이 된다. 친밀도는 걸음·평온의방울·럭셔리볼로 여전히 오른다
(§3.5).

**그래서 화석과 진화의돌은 백화점으로 갔다** (§12.1). 그쪽으로 **못 박았다** —
이 건물은 되살리지 않는다.

**닫는 방법은 원작의 문장이다.** 우리가 문을 막지 않는다. 리본 명령 일곱을
만들고 **읽는 넷이 0을 답하게** 했다 — 1층 들머리가 그 0을 10과 견주고 제
대사로 돌려보낸다 (`engine/script/ribbon.test.ts`가 그 0이 실제로 닿는지 잰다).
지어낸 글이 한 줄도 없다.

⚠️ **안 만들면 우연히 열린다.** 명령을 건너뛰면 결과 변수에 앞 갈래가 쓴 값이
그대로 남고, 거기 10 이상이 들어 있으면 문이 열린다. 「없다」도 **답을 해야**
없는 것이 된다.

⚠️ **리본을 만지는 자리는 이 건물 밖에도 있다.** 실측으로 스크립트 파일 다섯이다:

| 파일 | 무엇 |
|---|---|
| `scripts_resort_area_ribbon_syndicate_1f` | 들머리 판정 + 리본 가게 |
| `scripts_sunyshore_city_northeast_house` | 요일 리본 일곱 |
| `scripts_sunyshore_market` | 노력리본 (노력치 510) |
| `scripts_footstep_house` | 발자국리본 |
| `scripts_contests` | 대회 시상 |

붙이는 쪽 셋(`SetPartyMonRibbon`·`SetRibbonName`·`BufferRibbonName`)은 아무 일도
안 한다. 저장할 칸이 없으니 **붙였다고 적지 않는다.** 이름 칸은 비워 둔다 —
앞 명령이 넣어 둔 남의 이름이 그대로 뜨는 것이 더 나쁘다.

### 9.3 배틀팩토리 — 시설 다섯 중 하나를 되돌린다

배틀프런티어를 통째로 접어 두었는데, **파이트에리어 계통을 다 만들기로 하면서
배틀팩토리 하나를 §9에서 꺼냈다.** 다섯 중 이것을 고른 이유는 셋이다 — 규칙이
§2(배틀) 위에 그대로 얹히고, 짝이 없어도 성립하고(멀티만 §9), **BP를 벌 유일한
길**이라 §12.3의 교환 코너가 여기서 열린다.

**빌린 여섯에서 셋을 고른다.** 일곱 판을 이어 싸우고 이길 때마다 **쓰러뜨린
상대의 한 마리와 내 한 마리를 바꿀 수 있다**. 일곱을 다 이기면 라운드가 끝나고
BP를 받는다.

⚠️ **한 판의 도전이 곧 한 라운드다.** 라운드를 마치면 로비로 돌아가고 다음
라운드는 **새 도전**으로 시작한다 — 그때 개체를 다시 빌린다. 연승이 이어지는
것은 세션이 이어져서가 아니라 리포트에 **연승 표식**이 서기 때문이다
(`BattleFactoryStreakFlags`): 라운드를 마치면 1이 서고 지면 0이 된다.
⚠️ **이 비트가 없으면 라운드마다 연승이 끊긴다** — 값만 저장하면 진 사람도
이어서 도전하게 된다.

**표 넷을 오버레이 104의 코드에서 손으로 옮겼다** (`engine/frontier/factoryTables`).
아카이브에 없어서 추출기가 못 세어 준다 — BP 값 표와 같은 처지다 (§12.3):

| 표 | 무엇 |
|---|---|
| `Unk_ov104_022401FC` | 라운드 여덟 줄 × 구간 둘. **라운드의 마지막 판만 위쪽 구간**에서 뽑는다 |
| `Unk_ov104_0224028C` | 레벨50 — **빌리는 것과 상대 것이 같은 표다** |
| `Unk_ov104_0224023C` | 오픈레벨 — 빌리는 것 |
| `Unk_ov104_022402DC` | 오픈레벨 — 상대 것 |

⚠️ **같은 표를 두 방식으로 읽는다.** 빌릴 것을 뽑을 때는 층을 **라운드 번호로
바로 집고**(`ov104_0223A8F4`), 상대 것을 뽑을 때는 **트레이너 번호의 문턱**으로
찾는다(`ov104_0223A8A8`). 한쪽으로만 읽으면 시설장이 첫 층에서 나온다.

⚠️ **종족도 도구도 겹치면 안 된다.** 이미 뽑은 것과도, 판에 있는 것과도 겹치면
버리고 다시 뽑는다 (`ov104_0223A918`) — 그래서 여섯 마리가 도구 여섯 가지를
나눠 든다. 구간은 **위에서 아래로** 집는다 (`to - rand(to - from + 1)`).

⚠️ **교환을 많이 하면 마지막 몇 자리를 한 층 위에서 뽑는다** (`Unk_ov104_022401E8`
= 6·13·20·27·34). **문턱 「이하」인 첫 자리의 번호가 곧 개수라 여섯 번까지는
하나도 안 올라간다** — 한 칸 밀려 읽기 쉬운 자리다. 좋은 것을 주는 장치가
아니다: 위 층은 상대에게도 걸리므로 판이 통째로 세진다.

**개체값도 노력치도 형 표에 없다.** 개체값은 뽑은 층이 정한 값(0·4·8·12·16·
20·24·31)을 여섯에 똑같이 넣고, 노력치는 `evFlags`가 켠 능력에 **510을 나눠**
넣는다 — 둘이면 255가 아니라 **252**다(한 능력 상한). 성격은 표에 있고 PID는
그 성격이 나올 때까지 굴린다 (4세대는 PID가 성격을 정한다).

| | |
|---|---|
| 판 수 | 라운드마다 일곱 |
| 레벨 | 레벨50 → 50 · 오픈레벨 → 100 |
| 데리고 가는 수 | 싱글·더블 셋 (멀티 둘은 §9) |
| 시설장 | 소튼 — **스물한·마흔아홉 판째**. 은·금 인쇄가 붙는다 |
| BP | 라운드 1~4 5 · 5~6 7 · 7 8 · 8+ 9. **인쇄 판은 20** |
| 리포트 | 판 29에 넉 줄 (레벨50·오픈레벨 × 싱글·더블) |

⚠️ **프론티어 판은 리포트에 아무것도 안 남긴다.** 경험치도 상금도 도감도 노트도
포켓루스도 도롱마담 옷감도 안 돈다 — 원작이 `BATTLE_TYPE_FRONTIER_*`로 거른다.
레벨이 고정인 판에 경험치를 주면 그 자리에서 규칙이 무너진다. 체력과 PP는 **판
앞에서 통째로 회복된다** (`Party_HealAllMembers`).

⚠️ **개체 형 951과 트레이너 315를 새로 추출했다** (`data/frontier.json`, 138KB —
DATA.md §2.24). 트레이너의 `sets`는 **배틀타워 것이라 팩토리가 안 쓴다** —
팩토리는 상대가 데리고 나올 것을 그 자리에서 형 표에서 뽑고, 시설장 소튼이
세트를 0개 가진 것이 그 증거다.

⚠️ **화면은 BDSP 배치다** (§12.4). 원작은 위 화면에 여섯이 늘어서고 아래 화면이
그 하나의 값을 보여 주는 두 화면짜리라, 한 화면에서는 성립하지 않는다.

## 10. 스크립트 명령 — 계통별 결손

명령 840종 중 425종을 만들었고 **닿는 자리 55,463개 중 54,285개(97.9%)**가
돈다. 남은 1,178자리를 계통으로 갈랐다 (`.audit/probe/gapBuckets.mjs`).

⚠️ **97.9%의 분모에는 §9(범위 밖)가 들어 있다.** 한 수만 적으면 오해를 부르므로
둘을 같이 적는다 — 범위 밖은 아래 표의 **네 계통을 통째로** 뺀 것이다:
통신·WiFi 145 + 배틀타워 169 + 콘테스트 145 + 지하통로 50 = **509자리**.

| | 못 도는 자리 | 비율 |
|---|---|---|
| 전체 | 1,178 | 97.9% |
| **범위 안** (55,463 − 509 = 54,954) | **669** | **98.8%** |

⚠️ **리본 38자리가 이 표에서 빠졌다.** 접은 계통인데도 명령을 만들었기
때문이다 — 「리본이 없다」는 답을 해야 리본신드롬이 제 대사로 닫힌다 (§9.2).
**만들었다고 리본이 생긴 것이 아니다.**

⚠️ **아래 표의 계통 이름은 정규식이 붙인 것이라 정확하지 않다.** 「콘테스트」에
리본이, 「지하통로」에 별장 가구와 백화점 장식품이 들어와 있다 — 명령 이름까지
세어 가른 것은 §9.1이다.

| 계통 | 자리 | 명령 | 어디로 |
|---|---|---|---|
| 알 수 없음 (디컴프도 이름을 못 붙였다) | 226 | 70 | 이름이 `ScrCmd_1DF` 꼴이다. 부르는 자리를 읽어야 안다 |
| 콘테스트 | 145 | 45 | ⊘ §9. 장식 넷과 리본 일곱은 만들었다 (§7.16 · §9.2) |
| 배틀타워·프론티어 | 169 | 20 | ◐ **배틀팩토리와 교환 코너가 돈다** (§9.3 · §12.3). 나머지 넷과 멀티는 §9 |
| 기타 | 143 | 68 | 낱개다. 위 계통이 붙을 때 딸려 온다 |
| 통신·WiFi | 145 | 28 | §9 |
| 모험노트·기록 | 109 | 12 | 노트는 됐다(§7.4). 남은 것은 게임 기록·랭킹·TV(§7.5) |
| 엘리베이터·장치 | 12 | 3 | 승강기 그림 몇이 남았다 (§1.23). **전망대 둘은 안 만들기로 했다** (§7.7) |
| 지하통로 | 50 | 15 | ⊘ §9. **섞여 있다** — 본체·UG 인사말 42에 별장 가구가 붙어 있다 (§9.1). 백화점 계산대는 됐다 (§12.1) |
| 배틀 진입 | 27 | 5 | `StartWildBattle`(전설)과 다가오는 트레이너가 남았다. **눈이 마주칠 때의 곡은 됐다** — 갈래마다 다른 곡이고 사천왕 넷은 한 곡, 챔피언만 다르다 (`world/trainerEncounterBgm`) |
| 카메라·연출 | 40 | 11 |  |
| 메뉴·화면 | 34 | 8 | 리포트 아이콘 셋과 `SetMonSummary` — §5가 붙으면 같이 붙는다. `OpenBag`·`GetSelectedItem`은 됐다 (§4.6이 쓴다) |
| 로밍·특수출현 | 16 | 4 | §6. 꿀 나무 넷과 안농 글자 수는 됐다 (§6.6 · §6.8). 레이더 둘은 공용 스크립트라 여기 안 든다 |
| 상호교류광장 | — | — | ✅ 여섯 다 돈다. 장식 케이스까지 닫혔다 (§7.8 · §7.16) |
| 사파리·대습초원 | 11 | 4 | §7.7. 사파리 다섯은 됐다 — 남은 것은 파르크 목장 넷뿐이다 (GBA 연결이 있어야 한다, §9) |
| 교환(NPC) | — | — | ✅ 넷 다 돈다 (§1.24). 말 안 듣기(§2.18)가 이제 화면에 뜬다 |
| 나무열매 | — | — | ✅ 아홉 자리가 다 돈다 (§4.6) |
| 기술가르침·되살리기 | 17 | 7 | 조각을 받는 기술가르침(`PayShardCost`·`ShowMoveTutorMoveSelectionMenu`)이 남았다. **조각 살 곳은 열어 뒀다** (§12.3) |
| 도감 | 16 | 8 | §6.2 (화면은 §5에서 다 됐다) |
| 시계·날씨 | 13 | 3 | §8.3 |
| 육성가·알 | 12 | 4 | §3.2·§3.3 |
| 돈·코인·게임코너 | 12 | 6 | 게임코너 상품표와 축복시티 복권만 남았다 (§7.6). 백화점 4층 계산대는 됐다 (§12.1) |
| 글 칸 채우기 | 8 | 1 | `BufferCustomMessageWord` 하나. 낱말 고르기(⑤-6)가 붙어야 뜻이 생긴다 |
| 파티 조회 | 3 | 3 | 요약 화면의 기술 고르기 둘과 발자국 갈래 하나 |
| 필드 기술·탈것 | — | — | ✅ 일곱 다 만들었다 — 파도타기·폭포오르기·록클라임·괴력·자전거 |
| 포켓치 | — | — | ✅ 여섯 다 만들었다 (§7.3) |
| 명예의 전당 | — | — | ✅ 여섯 다 만들었다 (§7.11) |

## 11. 재는 법

이 문서의 수는 전부 다시 잴 수 있다. 지어낸 수는 없다.

| 무엇 | 어떻게 |
|---|---|
| 스크립트 명령 결손 | `npx vitest run .audit/probe/scriptGap.test.ts` → `node .audit/probe/gapBuckets.mjs` |
| 맵 270개 (건물·턱·NPC) | `node .audit/probe/mapAudit.mjs` |
| 숲 바닥 메우기 | `npx vitest run src/scene/plates.test.ts` |
| 배틀 불변식 | `BATTLE_SOAK=1500 npx vitest run src/engine/battle/sim/soak.test.ts` |
| 진화·트레이너·종족 수 | `public/data/*.json`을 직접 센다 |
| 결손이 **어느 맵**에 있는가 (§9.1) | `npx vitest run .audit/probe/whereGap.test.ts` → `where-gap.json` |
| 한 명령이 **어느 파일에서 같이 쓰이는가** | `npx vitest run .audit/probe/cmdWhere.test.ts` → `cmd-where.txt` |
| 도구 하나의 **입수지 전부** (§4.9) | `npx vitest run .audit/probe/itemSource.test.ts` · `literalScan.test.ts` |
| 리본신드롬이 **정말 닫히는가** (§9.2) | `npx vitest run src/engine/script/ribbon.test.ts` |
| 교환 진화 열일곱이 **다 열리는가** (§12.2) | `npx vitest run src/engine/pokemon/evolution.test.ts` |

⚠️ **`.audit/`는 Git 밖이다.** 탐침은 남겨 두지만 커밋되지 않으므로, 여기 적힌
수가 곧 그 실행의 결과다 — 수를 고칠 때는 다시 돌려서 고친다.

## 12. 원작에 없는 것을 더한다 — 이유를 적는다

⚠️ **이 문서의 나머지는 전부 「원작에 있는데 우리에게 없는 것」이다. 여기만
반대다.** 그래서 규칙을 좁게 둔다 — **원작이 열어 두었던 길이 §9 때문에 막혔을
때, 그 길만 도로 여는 것**만 더한다. 원작에 없던 재미를 보태지 않는다.

| 더하는 것 | 무엇을 도로 여는가 |
|---|---|
| **연결의 끈** (Linking Cord) | 「그냥 교환」 **넷** — 윤겔라→후딘 · 근육몬→괴력몬 · 데구리→딱구리 · 고우스트→팬텀 (`EVO_TRADE`) |
| **지닌 도구 열하나** (금속코트·왕의징표석·프로텍터…) | 「지닌 채 교환」 **열세 갈래**. 그 도구를 **쓰면** 걸린다 (§12.2) |
| **고운 비늘** (Prism Scale) | 빈티나 → 밀로틱. 원작은 아름다움 170인데 그 값을 올리는 길이 포핀뿐이고 콘테스트가 §9다 |

교환 진화는 통신(§9)에 매여 있어 **열여섯 종·열일곱 갈래가 영영 안 걸린다**
(실측: 그중 신오도감 안이 열둘이고, 진화형이 야생에도 뜨는 것은 강철톤뿐이다).

⚠️ **더한 도구는 둘뿐이다.** 열한 가지 지닌 도구는 롬에 이미 있는 물건이고,
바뀐 것은 **쓸 수 있게 된 것**뿐이다. 둘 다 원작 이후 세대의 물건이다 —
연결의 끈은 레전드 아르세우스, 고운 비늘은 5세대다. **지어낸 물건이 아니라 시리즈가 같은 문제를 풀려고 나중에
내놓은 답**이라 그것을 빌린다. 새 이름을 짓지 않는다.

⚠️ **롬의 도구 표 468종 밖의 번호를 쓴다.** 이름·설명·아이콘이 플래티넘 롬에
없으므로 우리가 붙여야 한다 — 그 자리는 「롬에서 뽑은 것」이 아니라는 표시를
자료 쪽에 남긴다 (DATA.md). 롬 번호를 덮어쓰지 않는다.

**셋 다 돈다.** 진화는 `pokemon/evolution`이 **종족 표를 안 고치고** 거는 쪽만
대신한다 — 표를 고치면 어느 줄이 원작인지가 사라진다. 어디서 얻는지는 §12.1,
무엇이 무엇을 여는지는 §12.2다.

### 12.1 어디서 얻는가 — 장막백화점 4층

⚠️ **지하통로가 넷 다의 원래 밭이었다** (§9). 화석·진화의돌·조각을 거기서
캤는데 그쪽을 접었으니 갈 데가 없어졌다. 실측:

| | 원작의 입수처 | 접은 뒤 |
|---|---|---|
| 화석 7 | 지하통로 발굴 | **0곳** |
| 진화의돌 9 | 지하통로 + 숨은 도구 | 종류마다 1~2곳, **일회성** (빛·어둠·각성은 각 1곳) |
| 조각 4 | 지하통로 + 숨은 도구 | 4~5곳, 일회성 |

⚠️ **새 사람을 세우면 롬에 없는 대사를 지어내야 한다.** 그래서 **이미 있는
계산대**에 재고만 얹었다 — 장막백화점 4층의 `PokeMartDecor`다. 원작 재고가
별장 가구인데 그 별장이 §9로 나가면서 **창구가 비어 있었다**. 문도 대사도
계산대 소리도 전부 롬 것 그대로다.

파는 것: **화석 일곱 · 진화의돌 아홉 · 연결의 끈 · 고운 비늘**
(`bag/evolutionCounter`). 값은 롬의 도구 자료에 있는 것을 쓰고, 롬 밖의 둘만
진화의돌과 같은 2,100원으로 적었다.

⚠️ **조각은 안 판다.** 그쪽은 기술가르침(§10)이 붙을 때 같이 정한다.

⚠️ **아이콘은 롬 그림이고 아틀라스에 제 칸이 있다.** 도구가 참조하지 않는
NCGR이 33개 남아 있는데(전부 3세대 잔재다) 연결의 끈은 그중 **381번** —
초록 화면이 달린 손에 드는 기계다. 고운 비늘은 하트비늘과 같은 그림을 제 칸에
다시 그린다. 아틀라스는 24×20 격자라 468·469가 이미 빈 칸으로 있었고, 굽는 쪽
둘(`tools/extract/itemIcons.js`와 브라우저 설치의 `import/platinum/icons.ts`)이
**같이** 두 칸을 더 그린다 — 한쪽만 고치면 롬으로 설치한 사람에게만 빈 칸이 뜬다.

⚠️ **롬에 GB플레이어 아이콘은 없다.** 아이콘 328장의 이름을 다 훑었고 안 쓰이는
33장도 눈으로 확인했다 — 게임보이는 한 장도 없다(그 도구 자체가 HGSS 것이다).
381번이 롬 안에서 제일 가까운 그림이라 그것을 쓴다.

### 12.2 무엇이 무엇을 여는가 — 도구가 교환을 대신한다

교환 진화는 **갈래가 둘**이다. 하나로 뭉치면 안 되는 이유가 실측에 있다:

| 갈래 | 수 | 무엇이 여는가 |
|---|---|---|
| `EVO_TRADE` (그냥 교환) | 종 4 · 갈래 4 | **연결의 끈** |
| `EVO_TRADE_WITH_HELD_ITEM` | 종 12 · 갈래 13 | **그 지닌 도구를 쓴다** |

⚠️ **끈 하나로 다 열면 도구 열한 가지가 갈 곳을 잃는다.** 금속코트·왕의징표석·
프로텍터·용의비늘·에레키부스터·마그마부스터·업그레이드·괴상한패치·영계의천·
심해의이빨·심해의비늘 — 원작에서 이것들이 하는 일이 **오직 그 진화**인데,
끈이 대신하면 가방에서 영영 안 쓰이는 물건이 된다.

지닌 채 교환하던 열세 갈래:

| 쓰는 도구 | 누가 |
|---|---|
| 금속코트 | 롱스톤→강철톤 · 스라크→핫삼 |
| 왕의징표석 | 슈륙챙이→왕구리 · 야돈→야도킹 |
| 심해의이빨 / 심해의비늘 | 진주몽→헌테일 / 진주몽→**분홍장이** |
| 프로텍터 | 코뿌리→거대코뿌리 |
| 용의비늘 | 시드라→킹드라 |
| 에레키부스터 | 에레브→에레키블 |
| 마그마부스터 | 마그마→마그마번 |
| 업그레이드 | 폴리곤→폴리곤2 |
| 괴상한패치 | 폴리곤2→폴리곤Z |
| 영계의천 | 미라몽→야느와르몽 |

⚠️ **진주몽 하나가 도구로 갈린다.** 이빨이면 헌테일, 비늘이면 분홍장이다 —
「지닌 것」을 안 보고 「쓴 것」으로 가르므로 이 갈림이 그대로 산다.

⚠️ **쓸 때 지님은 안 본다.** 가방에서 꺼내 쓰는 것이라 그 도구가 이미 손에
있다 — 지님까지 요구하면 같은 물건을 **둘** 모아야 한다. 원작이 요구한 둘
중 「교환」 자리를 「쓴다」가 메우는 것이지, 조건을 하나 지우는 것이 아니다.

⚠️ **목록을 손으로 안 적는다.** 종족표에서 `TRADE_WITH_HELD_ITEM` 갈래의
`param`을 모아 열한 가지를 얻는다 (`pokemon/evolution`의 `tradeEvolutionItems`).
가방이 그 값을 봐야 열린다 — 원작의 `fieldUseFunc`가 0(밖에서 못 쓴다)이라
표만 보면 눌러도 아무 일이 안 일어난다.

⚠️ **변함없는돌은 그대로 막는다.** 원작은 「도구로 거는 진화」만 통과시키는데
(`context != ITEM`), 우리는 교환과 아름다움을 도구로 걸므로 **갈래가 아니라
걸린 방법으로** 판단한다 — 안 그러면 돌을 물려 둔 고우스트가 끈에 진화당한다.
윤겔라만 예외인 것은 원작이 이 종 하나를 따로 적어 뒀기 때문이다.

**고운 비늘도 같은 규칙이다.** 아름다움 170을 대신하므로 레벨업 갈래를 열고,
변함없는돌이 막는다.

### 12.3 조각은 BP로 산다 — 배틀프런티어 교환 코너

기술가르침 셋(선단시티·212번도로·서바이벌에리어)이 조각으로 값을 받는다
(`PayShardCost`). ⚠️ **원작에서 조각을 캐던 곳이 지하통로였고 그쪽이 §9다** —
숨은 도구가 종류마다 네다섯 곳에 일회성으로 남을 뿐이라, 그것만으로는
기술가르침이 한 번 쓰고 끝난다.

**교환 코너를 열었다.** 원작의 `PokeMartFrontier`가 여는 창구 둘이 배틀파크와
배틀프런티어에 있고, 재고도 값도 롬 코드에 박혀 있다 (`scrcmd.c`의
`BattleFrontier…ExchangeServiceCorner`, `overlay007/shop_menu.c`의
`itemToBpPrice`). 그것을 그대로 옮기고 **조각 넷만 얹었다**.

| 창구 | 무엇 |
|---|---|
| 오른쪽 (0) | 기술머신 열다섯. 32·40·48·64·80 BP |
| 왼쪽 (1) | 영양제 여섯(1) · 파워 계열과 구슬 여덟(16) · 허브 둘(32) · 지닌 도구 열(48), 그리고 **조각 넷(1)** |

⚠️ **조각 값 1 BP는 우리가 정했다.** 원작에 조각의 BP 값이 없으므로 **새 층을
만들지 않고** 제일 싼 층(영양제 여섯)에 맞췄다. 지어낸 값을 최소로 두는 쪽이다.

**BP는 배틀팩토리가 준다** (§9.3). 한 라운드(일곱 판)를 마치면 5~9 BP,
스물한·마흔아홉 판째의 시설장을 이기면 20 BP다. ⚠️ **나머지 넷은 아직 §9라**
`GiveBattlePoints`는 필드 스크립트에 여전히 0회다 — 원작도 시설 코드가 직접
준다.

⚠️ **그래도 명령 일곱을 다 만들었다.** 안 만들면 교환 코너가 **묵은 변수 값**
으로 갈라진다 — 리본에서 밟은 함정과 같다 (§9.2). 읽는 쪽은 실제 값을 주고
쓰는 쪽은 실제로 깎는다.

**리포트에 칸이 하나 늘었다** (판 28, `battlePoints`). 상한 9,999는 원작
그대로고, 모자란 채로 깎으면 0이 된다 (`WifiBattleTowerRecord_UpdateBattlePoints`).
팩토리 기록은 판 29에 따로 들어갔다 (§9.3).

⚠️ **이 가게에는 「판다」가 없다.** 원작의 갈래가 「산다·그만둔다」 둘뿐이라
(`shop_menu.c`의 `maxOptions = 2`) BP는 되팔 수 없다.

### 12.4 두 화면짜리 화면을 한 화면으로

배틀팩토리의 고르는 화면은 원작이 **두 화면을 같이 쓴다** — 위에 여섯이
늘어서고 아래에 커서가 놓인 하나의 값(기술 넷·능력치 여섯)이 뜬다. 한 화면에
그 배치를 그대로 옮기면 목록도 값도 둘 다 좁아진다.

**BDSP의 대여 화면을 따랐다** — 왼쪽에 목록, 오른쪽에 그 하나의 값. 포켓치가
같은 이유로 BDSP 배치를 따르고 있어(§7.3) 화면끼리 어긋나지 않는다.

⚠️ **글은 원작 것이다.** 뱅크 364(고르는 화면 27줄)·365(복도와 배틀룸 40줄)를
새로 굽는다 — 배치가 바뀌어도 문장은 지어내지 않는다.
