# 고칠 것 — 신규 요소의 우려 지점과 구현 계획

**플래티넘에서 레디언트 플래티넘으로 오면서 새로 생긴 것**만 다룬다. 원작에
있는데 우리에게 없는 것은 [PARITY.md](PARITY.md), 원작에 있는데 옮기면 안 되는
것은 [BUGS.md](BUGS.md), 3D로 세울 것이 남았는가는
[3D_GAP_AUDIT.md](3D_GAP_AUDIT.md)가 정본이다. 여기 있는 것은 그 셋 어디에도 안
들어가는 갈래다 — **우리가 만든 자리가 우리가 만든 이유로 어긋난 것.**

- 항목마다 **무엇이 어긋났나(실측) · 어디를 고치나 · 어떻게 · 같이 깨지는 자리 ·
  재는 법**을 적는다. 어디를 고치는가의 정본은 [CODEMAP.md](CODEMAP.md)이고,
  여기서는 그 표에 없는 새 자리만 짚는다.
- 짐작으로 적은 줄은 없다. 수마다 잰 자를 옆에 적었다.
- **다 하면 이 문서는 빈다.** 빈 문서는 지운다.

---

## 0. 한눈에

| | 무엇이 어긋났나 | 값 | 순서 |
|---|---|---|---|
| §3 | 연기를 굽는 근사가 또도가스의 몸을 통째로 가린다 | 중간 | 2 |
| §8 | 맵을 나갈 때 셰이더 링크로 6.6초 멎는다 · 드로우콜은 같은 재질 메시가 둘씩이라 넘는다 | 중간 | **1** |

순서를 정한 잣대는 하나다 — **배포본에서 게임이 안 끝나는 것부터.**

---

## 3. 연기·불꽃을 굽는 근사가 화면을 덮는다

또도가스(`pm0110`)가 배틀에서 **몸을 통째로 가리는 거의 불투명한 회색 구**로
선다 (`shots/out-110.png`으로 봤다). 원작은 보라색 가스 두 덩이에 옅은 연기다.

⚠️ **굽는 쪽 둘이 갈린 것이 아니다.** 노드 추출기와 브라우저 변환기가 같은 값을
내고(그림 7장 · 최대 채널 차 2/255), **둘 다 이렇게 그린다.** 우리가 고른
근사가 화면에서 틀린 것이다.

**잰 것** (`.audit/smokeMat.mjs`):

| 재질 | 무엇이 적혀 있나 |
|---|---|
| `SmokeCoreA`·`SmokeCoreB00`·`SmokeCoreB01` | `_BaseColor`(0.850, 0.820, 0.615) · `_LayerColor`(0.597, 0.550, 0.264) · 모양은 `_Blend0Tex`(UV 배율 5×2) · 섞기는 `_Blend1Tex`(3×2) |
| 셋 다 | `_SrcBlend = 1`(One) · `_DstBlend = 0`(Zero) |
| `SmokeMaskB01` | `_BaseColor`가 **없다** — 스텐실로 깎아 내는 도구다 (안 그리는 것이 맞다) |

지금 굽는 법은 알파를 **`_Blend0Tex`의 광도**로 쓴다
(`import/bdsp/albedo.ts`의 `effectAlbedo` · `tools/extract/bdsp_bake_albedo.py`의
`effect_albedo`). 그 그림이 밝으면 알파가 1에 가까워져 **꽉 찬 면**이 된다.

**다음에 할 일 — 재는 것부터.** 짐작으로 알파를 낮추지 않는다.
① `_Blend0Tex`·`_Blend1Tex`·`_LerpTex`의 실제 픽셀 분포를 재고,
② 셰이더 키워드(`MASK_CALC_MODE_*`)와 `_SrcBlend`/`_DstBlend`가 원판에서
   무엇을 뜻하는지 확인한 뒤,
③ 그 값으로 알파를 정한다. 원판이 가산이면 glTF에 가산이 없으므로 **무엇을
   양보할지**를 골라야 한다 ([PLAN.md](PLAN.md)의 「읽히는 쪽이 이긴다」).

⚠️ **한쪽만 고치면 안 된다.** 굽는 쪽이 둘이므로 파이썬과 타입스크립트를 같이
고치고, `monModels`의 `GROUP_FORMAT`을 올린다.

---

## 8. 성능 — 맵 전환 멈춤 · 드로우콜

**재는 자는 고쳤다.** 배틀 장면이 실행마다 통과와 실패를 오가던 것은 성능이
아니라 자였고(`story.mjs`가 배틀이 **시작도 안 한** 상태를 「돌기 시작했다」로
읽었다), 고치고 나니 배틀 장면 열여섯이 끊김 0이다. 그 이야기와 실측은
[DEPLOY.md](DEPLOY.md)의 스윕 절이 임자다.

**남은 것은 둘이다.** 값은 그 절에서 온다 — 여기 베껴 적지 않는다.

### 8.1 맵을 나가는 동안 멎는다 — **셰이더 링크다**

**쟀다** (`.audit/warpCost.mjs`, 진짜 GPU · `--use-angle=d3d11 --enable-gpu`):

| 어디 | 멈춘 프레임 | 합 | 제일 긴 것 |
|---|---:|---:|---:|
| `forest` (맵 203 → 347) | 7 | **6.58초** | **3,833ms** |
| `door` (맵 414 안에서) | 2 | 0.95초 | 517ms |

**둘을 가른 것이 답이다** — GL 호출 시간:

| | `forest` | `door` |
|---|---:|---:|
| `getProgramParameter` | **53회 1,028.8ms** | **0회** |
| `bufferSubData` | 40,609회 149.9ms | 13,437회 17.5ms |
| `bufferData` | 65,355회 115.9ms | 51,660회 46.9ms |
| `texSubImage2D` | 120회 **2.1ms** | 0회 |

⚠️ **문서가 세워 뒀던 후보 둘이 다 틀렸다.** 청크 메시를 만드는 CPU도(우리 함수
자기 시간은 전부 40ms 아래다), 텍스처를 올리는 GL도(`texSubImage2D` 120회에
2.1ms — 사실상 공짜다) 아니다. **`getProgramParameter`가 드라이버의 링크를
기다리며 막는 것**이고, 그 뒤에 네이티브 `run` 2,447ms가 붙는다. 새 맵이 새
재질 짝을 데려오면 그만큼 새 프로그램을 굽는다 — `door`는 이미 데워진
프로그램만 써서 그 자리가 0회고, 그래서 517ms다.

⚠️ **스윕의 값과 다르다.** 스윕은 SwiftShader 폴백이라 `door`가 5.92초로 잡혔다.
같은 자리가 진짜 GPU에서는 517ms다 — **어디가 아픈지는 스윕으로 못 고른다.**

**우리는 이 병을 이미 안다.** 배틀이 열릴 때 같은 자리에서 막혔고
(`.audit/battleWarm.mjs`), `scene/warmPipelines.ts`가 `compileAsync`로 미리
구워 두는 길을 냈다 — `warmBeforeShow`·`addWhenWarm`이고 인물(`NpcModels`)과
포켓몬(`NpcMonModels`)이 쓴다. **맵 청크가 그 길을 안 쓴다.**

#### 해 봤고 **안 통한 것** — 지형만 데우기

지형 조각(`ChunkModels`의 `placed`)을 임시 그룹으로 묶어 `warmBeforeShow`로
미리 굽고 나서 세워 봤다. **거의 안 줄었다:**

| | 전 | 미리 굽고 |
|---|---:|---:|
| 멈춤 합 | 6.58초 | 6.33초 |
| 제일 긴 프레임 | 3,833ms | 3,633ms |
| `getProgramParameter` | 53회 1,028.8ms | 55회 936.4ms |

**되돌렸다.** 까닭은 데운 것이 모자라서다 — 맵이 데려오는 새 재질은 지형만이
아니라 **나무·바위·풀·꽃·물·소품·인물**에도 있고, 막히는 링크는 그쪽에서 난다.

⚠️ **길 자체는 막혀 있지 않다.** `KHR_parallel_shader_compile`이 이 기계에
**있다**(`.audit/parallelCompile.mjs` — ANGLE/Intel Arc 140V, D3D11). three의
WebGL 백엔드는 그 확장이 있을 때만 링크 확인을 미루므로(`warmPipelines` 머리말)
`compileAsync`가 진짜 비동기로 돌 수 있다. **한 자리만 데워서는 안 되고 새
재질을 세우는 자리를 전부 그 길로 돌려야 한다**는 뜻이다.

**다음에 할 일.** 그 「전부」가 어디인지부터 센다 — 맵을 옮기는 동안 새로
링크되는 프로그램이 **어느 컴포넌트의 재질인지**를 `linkProgram` 앞뒤로
표식을 걸어 가른다. 그 목록이 나오면 `addWhenWarm`/`warmBeforeShow`를 그
자리들에 붙인다. ⚠️ **붙일 때마다 재고, 안 줄면 되돌린다** — 굽는 동안 그것이
늦게 떠서 **팝인**이 생길 수 있고, 그건 맞바꿀 값이 아닐 수 있다.

### 8.2 드로우콜이 예산을 넘는 자리 넷 — **같은 재질을 쓰는 메시가 둘씩이다**

예산은 300이고 `vsseeker` 346 · `berry` 336 · `poketch` 318 · `sandgem` 302가
넘는다 (스윕이 그 값의 임자다 — [DEPLOY.md](DEPLOY.md)).

**쟀다** (`.audit/drawSplit.mjs`, 진짜 GPU · 960×640). 렌더러가 센 콜 수를 임자로
두고 그 옆에 씬의 구성을 적는다 — 씬을 훑어 콜을 **다시 세면** 절두체 밖의 것까지
세어 진짜 값과 갈리므로 그렇게 안 한다:

| `vsseeker` (드로우콜 326) | 재질 칸 | 메시 |
|---|---:|---:|
| 지형 | 306 | 23 |
| **메운 바닥** | **304** | 22 |
| 소품 | 116 | 33 |
| **소품 채운 면** | **105** | 29 |
| 판 옆면 | 109 | 6 |
| 그 밖 | 170 | — |
| 합 | **1,110** | |

⚠️ **재질이 갈려서가 아니다.** `ChunkModels`·`Foliage`가 이미 인스턴싱과 절두체
컬링을 하고 있고(나무 964그루가 인스턴스 둘, 꽃 688송이가 넷), 남은 큰 덩어리는
**같은 재질 배열을 쓰는 메시가 둘씩 서 있는 것**이다:

- 「메운 바닥」은 숲 바닥의 구멍을 메우는 판인데 `p.materials`를 **지형과 그대로
  공유**한다. 그래서 청크마다 지형 13콜 + 바닥 13콜이 따로 나간다.
- 「소품」과 「소품 채운 면」이 같은 짝이다.

**다음에 할 일.** 청크마다 그 짝을 **한 기하로 합친다** — 재질 그룹
(`addGroup`)을 이어 붙이면 재질 배열은 그대로 두고 메시만 하나가 된다. 지형과
바닥이 610칸에서 306칸으로 줄고, 소품 짝이 221칸에서 116칸으로 준다.
⚠️ **합치고 나서 재고, 화면도 본다** — 「메운 바닥」은 지형보다 **뒤에** 그려져야
하는 자리가 있을 수 있고(§8.5의 문짝처럼), 순서가 바뀌면 바닥이 지형을 덮는다.

⚠️ **문지기는 고친 뒤에 붙인다.** `story.mjs`가 장면마다 드로우콜을 이미
재고 있으므로 상한을 넘으면 그 장면을 FAIL로 만들 수 있는데, 지금 넷이 이미
넘으므로 붙이는 순간 빨개진다. 그리고 다섯째(`grass` 299)가 문턱에
바싹 붙어 있어서 **붙이자마자 다섯이 될 수 있다.**

### 여기서 안 하기로 한 것

⚠️ **백엔드를 리포트에 안 적는다.** 사용자가 실제로 어느 백엔드로 도는지는
화면 구석 HUD가 이미 찍고 있다(`scene/EngineDriver`). 진단 화면에 한 줄
올리는 것으로 끝내고 리포트 스키마는 안 건드린다 — 건드리면 `SAVE_VERSION`이
오른다.

⚠️ **스윕 값은 WebGL2 폴백에서 잰 것이다.** 헤드리스 크로미움이 WebGPU 장치를
못 만들어 렌더러가 내려앉는다 — 사용자 브라우저의 수치가 아니다
([DEPLOY.md](DEPLOY.md)).

---

## 10. 순서와 값

| 순서 | 무엇 | 왜 이 자리인가 | 값 |
|---|---|---|---|
| 1 | §8 — 성능 | **둘 다 쟀다.** 8.1은 셰이더 링크, 8.2는 같은 재질을 쓰는 메시가 둘씩인 것 | 중간 |
| 2 | §3 — 연기 근사 | **화면에서 틀린 것이 보인다.** 다만 셰이더를 다시 읽어야 한다 | 중간 |

⚠️ **포켓몬 이로치·암컷 외형(231MB)은 보류하기로 정했다** (2026-08-19) —
[PLAN.md](PLAN.md) §16.10으로 옮겼다. 되살릴 때 `npcModels`의 `GROUP_FORMAT`을
올리는 배포에 묻어 실어야 사용자가 롬을 두 번 안 고른다는 것도 거기 적혀 있다.
