# 3D·시점 전환·게임 진행·배틀 연출 종합 검토 및 수정 지시 — 2026-09-19

## 0. 결론과 기준

**전체 완료 판정은 보류한다.** 일부 3D 개선은 완료됐고 대표 시점 전환도 작동하지만, 배틀 시간 동기화·포획 결과 순서에 재현 가능한 문제가 있다. 기존 네 건의 완료 보고를 전체 환경물·전체 플레이·전체 배틀 연출의 완료로 확대하면 안 된다.

검토 기준 커밋: `b8973b4dcc70b4a20eea1da1f64c2f6cd8424718`. 시작 시 작업 트리는 깨끗했다. 재개 시 HEAD는 `09e77d7`이며 직전 기준과의 차이는 문서 및 journey/evidence 하네스다. `src/` 제품 코드에는 차이가 없으므로 아래 제품 코드 재현과 check 결과의 대상은 동일하다. 새 하네스 전체 시험을 이번에 다시 실행했다는 뜻은 아니다. 이 검토는 제품 코드를 수정하지 않고 진단 스크립트·로그·화면과 본 문서만 작성했다. 기존 `FIRST_PERSON_VISUAL_IMPROVEMENT_PLAN.md`와 후속 결정서, 최신 구현 기록을 함께 대조했다.

### 최신 커밋 대조 — 6ec7fbd (2026-09-19 후속 갱신)

`09e77d7..6ec7fbd`를 대조했다. `src/` 변경은 없고, Netlify 제거/Cloudflare 배포 설정과 문서가 바뀌었다. 따라서 **R1~R6의 해결 상태와 기존 제품 코드 재현은 그대로**다. `a6b0fd4`는 본 검토 문서 반영 커밋이며 결함 수정 커밋이 아니다. 이번 후속 검토에서는 장시간 플레이나 전체 check를 다시 실행하지 않았고, 최신 `release:check`와 커밋 diff·증거 JSON을 확인했다.

- **새 완주 증거 존재:** `.audit/journey.json`의 testedAt은 `2026-09-19T08:32:04.030Z`, PASS 23, contract7, shortcuts=[]다. sourceDigest는 `c652443121e4`, buildId는 `b8973b4`로 적혀 있다. 실행 기록의 HEAD 설명과 buildId는 같은 필드가 아니므로 임의로 덮어쓰지 않는다. ‘완주한 판 자체가 없다’는 해석은 철회한다. 현재 판정기에는 최신 바인딩과 불일치해 유효 증거로 채택되지 않는다.
- **추가 수정 R7:** 최신 사탕 기본 판에서 좋은상처약을 넷 사고도 0회 사용했다는 기록이 있다. 기존 메뉴 오인 수정과 다른, 회복 주머니 첫 줄만 선택하는 하네스 제약이다. 아래 R7을 추가한다.
- **배포 경로:** Netlify 설정/야간 배포 도구는 삭제됐다. 복구하지 말고 현재 Cloudflare 설정·`public/_headers` 및 현행 배포 문서를 따라 검증한다. 호스트에 직접 배포하는 작업을 이 지시서가 새로 실행했다는 뜻은 아니다.
- **성능 판단 보완:** survival 재측정 통과 기록은 인정하되, 삼각형/드로우콜 감소만으로 성능 회귀를 배제할 수 없다. 자산 대기·컴파일·CPU 지연·culling도 관여할 수 있다. 동일 카메라/시각/자산 준비/로드 결과를 먼저 고정한 뒤 반복 측정한다. 불완전 화면은 성능 표본에서 따로 분류하되 로딩 지연/누락 자체는 문제로 남긴다.

증거 표기는 다음과 같다.

- **직접 실행:** 이번 검토에서 최신 소스로 실행했다.
- **코드 재현:** 제품 소스를 transpile해 제어된 의존성/시계로 재현했다. 브라우저 영상 검증과 구분한다.
- **기존 증거:** 이전 구현자의 자료를 읽거나 화면을 직접 열어 확인했다. 이번에 같은 장면을 재촬영했다는 뜻이 아니다.
- **미검증/범위 차이:** 현재 실패가 확정된 것은 아니지만 전체 완료를 뒷받침하는 구현 또는 증거가 부족하다.

## 1. 이번 검증 결과

### 1.1 정적 검사와 시험 — 통과

`pnpm check` 종료 코드 0. 타입 검사·lint·unused export 검사 통과. 일반 시험 **4,710 통과, 3 skipped**(363 파일 통과, 1 파일 skipped), 별도 shimmed 시험 **4 통과**. 로그: `.audit/review-20260919-check.log`.

시험 통과는 연출 완성도를 증명하지 않는다. 아래 시간차와 포획 순서 문제는 이 시험 묶음이 통과한 동일 코드에서 재현됐다. 배포를 하지 않았고 `pnpm build`도 이번 검토에서는 실행하지 않았다.

### 1.2 실제 WebGL 시점·걷기 — 대표 네 장소

실행: `node tools/audit/firstPersonAudit.mjs --sites=room,twinleaf,center,forest --capture=all --gpu=gl --yaws=0,90,180,270 --pitches=-25,0 --out=shots/first-person/review-20260919-gl`.

실제 backend는 네 장소 모두 `WebGLBackend`. V키로 3→1→3→1 전환 **12회 모두 성공**, 검사 도중 1인칭 유지. 정지 32장, 걷기 40장과 전환 화면을 저장했다. 콘솔/page 오류와 자홍 픽셀은 이 네 장소에서 0이었다. 서버 준비는 200초였다.

| 장소 | 시점 전환 | 보행 위치 변화 | 화면 판정 |
|---|---|---|---|
| 주인공 방 415 | 성공 | x 8.5에서 이후 4.94까지 이동 | CAPTURED. 초기 정지 화면에는 TV 대사가 있었으나 후속 보행 위치가 실제 변했다 |
| 떡잎마을 411 | 성공 | x 116.5에서 102.31까지 이동 | CAPTURED |
| 포켓몬센터 420 | 성공 | z 12.5에서 6.3까지 이동 | 남쪽 yaw180, pitch -25/0에서 FAILED_VISUAL. 직접 보면 검은 무출력 화면이 아니라 문/벽의 큰 단색 띠가 화면을 채운다 |
| 영원의 숲 203 | 성공 | z 85.5에서 82.3까지 이동 후 멈춤 | CAPTURED. 막힌 지점에 도달한 한 방향 보행으로 넓은 지역의 자유 이동을 입증하지 못한다 |

근거: `shots/first-person/review-20260919-gl/run.json`, 같은 디렉터리의 PNG, `.audit/first-person/baseline-gl-2026-09-19T06-28-19-503Z.html`.

`CAPTURED`는 촬영 성공이며 최종 품질 PASS가 아니다. 이 검사는 서 있는 상태의 토글과 짧은 보행이다. 워프·점프·surf·자전거·낚시·깨어진 세계·전투 도중 옵션 변경·세이브 복원까지 모두 확인한 것으로 확대하지 않는다. 창 크기·DPR은 1280×720/1이며 다른 비율은 미검증이다.

### 1.3 실제 WebGPU 야생 배틀 — 등장 확인

별도 브라우저에서 `wild` 확인 지점으로 진입했다. 실제 `WebGPUBackend`, `sceneReady=true`, 야생 꼬링크와 모부기 모델, 등장 글, 위협에 따른 공격 하락 글, 명령 메뉴까지 확인했다. pageerror 0. 70회 상태 표본 및 영상을 저장했다.

근거: `.audit/review-20260919-battle.json`, `shots/review-20260919-battle/`.

첫 진단 스크립트는 가방 버튼 이름을 완전 일치로 찾았으나 실제 버튼에는 부제도 있어 클릭하지 못했다. 따라서 이 첫 녹화는 **등장 관찰만**이며 기술·포획·종료 통과 증거가 아니다. 수집 중 다른 렌더 검사도 실행됐으므로 화면의 FPS 값을 제품 성능 합격/불합격으로 사용하지 않는다.

### 1.4 재개 후 실제 WebGPU 기술·도주 확인

재개 시 `09e77d7`의 제품 소스는 최초 검토 코드와 동일하다. 이미 떠 있던 localhost:8655 서버에 별도 브라우저로 접속했고 해당 서버는 종료하지 않았다.

`shots/review-20260919-resumed-action/`와 `.audit/review-20260919-resumed-action.json`: 실제 버튼으로 싸운다 → 몸통박치기 → 피해 처리 → 명령 메뉴 복귀를 확인했다. 이어 도주 실패 → 상대 몸통박치기 → 모부기 HP 26→21 → 메뉴 복귀도 확인했다. pageerror 0, 실제 WebGPUBackend. 첫 스크립트는 도주를 한 번만 시도하므로 필드 복귀를 확인하지 못했다. 성공하지 못한 도주를 시점 복원 결함으로 세지 않는다.

후속 `shots/review-20260919-exit-check/frame-90.png`에서는 **무사히 도망쳤다! → 계속 버튼**을 직접 확인했다. 이는 정상 종료 확인 화면이다. 해당 스크립트에는 계속 버튼 입력이 없어 필드 복귀 증거로는 부족하며, 마지막 return-final 실행에서 그 입력까지 포함해 아래 결과로 완료했다.

중단 전에 시작한 battle-action-retry는 최종 JSON 없이 로그만 남았으므로 미완료다. 첫 battle-action 실행의 page.goto 90초 timeout도 인프라 실패이며 제품 결함으로 세지 않는다. 실제 기술 한 가지의 처리 성공은 전체 기술의 완성도를 입증하지 않는다.

**마지막 직접 실행 완료:** `.audit/review-20260919-return-final.json`은 fightClicked/moveClicked/ran=true, runAttempts=1, 종료 phase=off, first=first, third=third, pageerror=[]이다. `shots/review-20260919-return-final/field-first.png`와 `field-third.png`를 직접 열어 필드가 렌더되고 1인칭에서 자기 몸이 숨고 3인칭에서 플레이어가 보이는 것을 확인했다. 즉 **실제 기술 사용 → 도주 → 계속 → 필드 복귀 → 1·3인칭 왕복** 대표 경로는 동작한다. 전투 직후의 캡처에는 컴파일/전환/동시 진단 부하가 섞여 있으므로 표시 FPS를 안정 성능 측정으로 쓰지 않는다. 승리·전멸·포획·1인칭 시작 전투까지 모두 검증한 것은 아니다.

이번 실행에서 별도 작성한 진단 스크립트는 화면 관찰용이며 `CAPTURED` 자체를 pass assertion으로 사용하지 않는다. 이 문서의 완료 판정은 실제 입력 flag·상태·화면을 함께 대조한 결과다.

## 2. 확정 문제와 수정 티켓

### R1 — P1: 배틀 재생기와 3D 연출의 시계가 다르다

위치: `src/ui/battle/useBattlePlayback.ts:41,172,197`, `src/scene/battle/MoveVfx.tsx:158`, `src/scene/battle/BattleStage.tsx:391`, `src/scene/battle/BattleBallEffects.tsx:126`.

재생기는 requestAnimationFrame 콜백마다 `r.wait--`한다. VFX와 몸은 delta 초, 공은 performance.now 초를 사용한다. 원작 60프레임을 디스플레이 60회와 동일시한 구현이다.

**코드 재현:** 실제 useBattlePlayback 소스를 TypeScript transpile 후 React hook과 RAF를 제어해, text=null인 120프레임 move 뒤 damage를 배치했다.

| RAF 주기 | 기준 연출 시간 | 피해 이벤트까지 실제 경과 |
|---|---:|---:|
| 30Hz | 2,000ms | 4,033ms |
| 60Hz | 2,000ms | 2,017ms |
| 120Hz | 2,000ms | 1,008ms |
| 144Hz | 2,000ms | 840ms |

재현: `node .audit/review-20260919-playback-clock.mjs`. 결과 JSON은 같은 이름이다. 이 값은 합성 RAF 재현이며 실제 144Hz 디스플레이 녹화 값은 아니다. 하지만 실제 함수가 refresh rate에 종속되는 것은 확인됐다.

수정 계약:

1. 프레임 수는 콘텐츠 데이터로 유지하되 실행은 공통 배틀 시계의 ms 또는 누적 60Hz 고정 step으로 바꾼다. 모니터 Hz를 추정해 상수를 보정하지 않는다.
2. 메시지 대기, HP gauge, 몸 동작, VFX, 공, 카메라를 동일한 시작 시각/진행률로 묶는다. `BattlePresentationClock` 같은 엔진 독립 모듈과 UI/씬 어댑터로 구현할 수 있다. UI가 scene 모듈을 import하는 역의존은 피한다.
3. 탭 숨김/복귀와 긴 프레임에서 여러 의미 있는 박자가 한 번에 소모되지 않게 한다. 연출 중 일시 중단 정책을 하나로 정하고 performance.now 기반 공에도 적용한다. 숨긴 동안 결과만 진행해 복귀 시 마지막 장면만 뜨면 안 된다.
4. 효과마다 seq와 start/complete 상태를 가진다. 완료 acknowledgement 또는 동일 timeline 종료를 기다린다. 모델 누락·실패는 명시적 fallback 완료 경로를 갖추고 무한 대기하지 않는다. timeout을 성공으로 위장하지 않는다.
5. `MoveVfx`의 `Shape`는 새 shot이 도중에 오면 useRef(t)가 재사용될 수 있다. 효과별 key/리셋과 오래된 done callback 차단을 넣는다. 이전 효과의 종료가 새 효과를 지우지 않게 한다.

검증: 30/60/120/144Hz 및 불규칙 frame timestamp에서 순서와 소요 시간이 같아야 한다(한 frame 수준 오차 허용). 이동/VFX 종료 전 HP가 바뀌지 않아야 한다. 배틀 속도 전 설정, A/Z 연타, 백그라운드 복귀를 포함한다. 실제 브라우저와 fake clock 결과를 별도로 제출한다.

### R2 — P1: 포획 결과 글이 공 연출을 기다리지 않는다

위치: `src/engine/battle/playback.ts` default 분기(`show([told],0)`), `src/ui/battle/messages.ts:660`, `BattleBallEffects.tsx`, `battleBallMotion.ts`.

ball 전용 박자가 없어 **ball 이벤트/hold0 → 포획 결과 문구/hold30**가 생성된다. 재생기는 hold0을 같은 RAF 안에서 이어 처리한다. 결과는 던지기·봉합·흔들기와 동시에 알려진다. 3회 흔들기 결과 시점은 현재 공 함수 기준 `0.92 + 3×0.46 = 2.30초`다.

**코드 재현:** `node .audit/review-20260919-capture-order.mjs`가 실제 buildBeats에서 위 배열을 출력한다. view helper는 stub이며 순서 검증용이다. 실제 포획 영상 재현은 별도 완료 조건이다.

수정 계약:

- ball 전용 분기로 **던짐 → 대상 수납 → 흔들기 0~3회 → 성공/탈출 확정 시각 → 결과 글 → 다음 행동/배틀 종료**를 만든다. 메시지 문자열 길이를 공 대기 시간으로 사용하지 않는다.
- captureResolveAt/captureDuration을 공통 표현 시간표로 옮기거나 같은 계약에서 생성한다. 현재 scene 함수에 엔진이 직접 의존하지 않게 한다.
- `animates`가 move만 보호하므로 switch/ball 등은 advance로 건너뛸 수 있다. `skippableText`와 `awaitPresentation`을 구분해 A/Z가 글 대기만 단축하도록 한다. 빠르기 설정도 실제 공보다 재생기만 빠르게 만들면 안 된다.
- 성공 시 닉네임/도감/배틀 해제, 실패 시 상대의 다음 기술은 포획 해소 뒤에만 진행한다. 마지막 frame을 보여 주기 전에 무대를 unmount하지 않는다.

시험: 0/1/2/3회 실패, 3회 성공, 보장 포획, 사파리, 가방 수량 감소 1회. 자동 진행과 연타 둘 다 검사하고 결과 문구 최초 시각이 결과 확정 시각 이상인지 타임라인으로 assert한다.

### R3 — P1: HP 감소와 기절 표현을 분리해야 한다

위치: `BattleStage.tsx:244,399,488`, `engine/battle/view.ts:239,277`, `engine/battle/playback.ts:242`.

현재 damage 이벤트가 hp=0을 적용하자마자 Slot은 `mon.hp<=0`으로 사라지기 시작한다. gauge는 holdMs 동안 CSS 전환을 하므로 HP가 내려가는 도중 몸이 사라질 수 있다. 별도 faint 이벤트를 기다리지 않는다. fade 중 `t<0.99`이면 motion이 `enter`로 선택되어 퇴장에도 등장 클립이 선택되는 구조다. faint 박자는 7프레임뿐이며 몸 fade는 0.35초다. **코드로 확인한 상태 결합 문제**이고 이번 실제 KO 연속 영상은 미검증이다.

수정 계약:

- 숫자 HP 목표와 화면상의 `alive/fainting/hidden` 상태를 분리한다. 피해 HP가 0이어도 gauge 완료 전에는 살아 있는 모습/피격 표현을 유지한다.
- 명시적인 faint presentation 이벤트로 기절 시작 → 완료 → 기절 글 → 경험치/교체를 연결한다. 이미 view의 fainted가 damage에서 true가 되므로 해당 boolean으로 갈아 끼우는 것만으로는 해결되지 않는다.
- 적절한 원본 faint clip이 없으면 명시적인 가라앉기/fade fallback을 사용한다. enter clip을 퇴장에 재사용하지 않는다. 상대와 내 포켓몬, 더블의 slot별 처리를 분리한다.
- `HOLD_FAINT=7`은 체력창 퇴장 값이라는 주석이 있다. 이를 몸 동작 완료 시간으로 취급하지 않는다. 엔진의 실제 전투 결과/기절 판정을 늦추지 말고 표시 순서만 제어한다.

완료: 큰 HP 감소 KO·독/화상 KO·반동/동시 KO·더블 한쪽 KO에서 gauge 종료 뒤 기절 시작, 모델 제거 뒤 교체가 순서대로 한 번만 일어나야 한다.

### R4 — P3: Scale2x가 미러 반복 경계를 일반 반복으로 읽는다

위치: `src/scene/chunkMesh.ts:390~462`.

sliceTexture는 wrap mode가 Clamp가 아니면 boolean true를 넘긴다. scale2x는 true이면 modulo로 반대쪽 끝 픽셀을 읽는다. 따라서 `MirroredRepeatWrapping`도 Repeat와 같아진다. 미러 경계의 바로 바깥 이웃은 반대편 끝이 아니라 경계 픽셀이어야 한다.

**코드 재현:** 실제 scale2x 함수에 불투명 4×4 경계 fixture를 넣었을 때 출력 (0,2)의 red가 2, 올바른 미러 경계에서는 1이다. `.audit/review-20260919-mirror.mjs/json` 참조. 추가로 현재 추출된 chunks/props/distortionProps/starter의 PT3C 1,301파일을 조사했으며 활성 미러 반복 재질은 0개였다(`.audit/review-20260919-mirror-usage.json`). 따라서 현재 화면 결함의 원인으로 확정하지 않는다. 합성 입력에서 확인한 지원 경로의 잠재 오류이므로 우선순위는 P3로 낮춘다.

수정: bool 대신 `clamp/repeat/mirror`를 축별 전달한다. mirror 좌표는 길이 2N 주기로 정규화한 뒤 N 이상이면 `2N-1-q`로 반사한다. 매 확대 단계에도 동일 모드를 유지한다. GPU texture.wrapS/T와 CPU 이웃 샘플의 의미를 일치시킨다. 음수·N·2N 경계, X/Y 독립, 투명 텍셀, 2회 확대를 시험한다. 실제 rep 미러 비트 자산을 골라 1·3인칭 이음 확대 캡처를 남긴다.

## 3. 3D 표현의 미완료/재판정 범위

### R5 — P2: 문간 화면과 천관산 흰 띠

- **직접 재촬영:** 센터 map420 (8.5,0,12.5), yaw180, pitch0에서 화면은 회색 세로 띠와 갈색 면으로 채워진다. `center/y180_pp0.png`를 직접 확인했다. 렌더러가 죽은 검은 화면과는 다르다. 근접한 문/벽이라는 원인은 합리적이지만, 자연스러운 1인칭 품질 문제는 남는다.
- **기존 화면 직접 검토:** `shots/first-person/fp-tail/coronet_220/y180_pm25.png`의 동굴 내부 흰 수평 띠가 실제로 보인다. 최신 실행 기록에도 메시 소유자를 찾지 못했다고 적혀 있다. 이 기존 파일은 현재 커밋의 새 캡처로 간주하지 않는다.
- **기존 진단 미해결:** 실내 틈/깨어진 세계 광선 결과는 `docs/DEPLOY.md §5.2`와 최신 FP 기록을 대조해야 한다. 오래된 전체 개수를 현재 결함 개수로 반복하지 않는다. 광선이 허공을 만났다는 이유만으로 열린 지역에 벽을 세우지 않는다.

구현 지시:

1. 실제 렌더에 쓰는 camera의 world/projection matrix로 픽셀을 역추적하고 삼각형 hit barycentric UV, material, 원본 sourceKey를 기록한다. 재구성 카메라와 잘못된 UV 중심값은 사용하지 않는다. 투명도·후처리 때문에 첫 ray hit와 화면색이 다를 수 있으므로 메시별 숨김 비교/오브젝트 ID 진단도 사용한다.
2. 센터는 입장 직후뿐 아니라 정상 보행으로 같은 칸에 접근·회전·퇴장해 보이는 범위를 확인한다. 플레이 가능 상태와 진단 워프 위치를 구분한다. 문/프레임 geometry와 벽 UV를 역할별로 나누고 인접한 실제 벽의 texel 밀도를 유지한다. 벽 전체에 문 무늬를 늘려 붙이거나, 화면을 숨기려 카메라 방향을 강제하지 않는다.
3. 동굴은 가해 mesh를 확정한 뒤 해당 면/소유권/깊이/UV를 고친다. 흰색만 다른 색으로 덮거나 전체 DoubleSide를 켜서 끝내지 않는다.
4. 양 backend, 0.5/1/2타일, 8방위, 1↔3 전환 및 실제 출입을 검증한다. 가까운 벽을 보는 장면 자체는 허용 가능하나 무엇을 보는지 식별할 수 있고 틈/무출력과 구별돼야 한다.

### R6 — P2: 전체 명세와 ‘네 건 완료’의 범위를 다시 맞춘다

현재 구현 기록의 2026-09-18 표는 자홍·볼라드·갈색 턱·카드 분류의 네 건을 말한다. 전체 명세의 아래 항목은 별도다.

| 원래 계약 | 현재 확인한 코드 | 후속 처리 |
|---|---|---|
| 나무의 검수된 수관/줄기 변형, 실제 bounds와 LOD | Foliage는 Icosahedron/Octahedron 덩어리, ballNormals, 고정 TREE_SPHERE=1.35 | §6.4/§10 대조. 실제 자연스러움과 culling/LOD를 재검수하거나 구현. 단순 다면체 사용 자체를 결함으로 단정하지 않되 완료 근거 필요 |
| 바위의 의미별 비대칭 형태·면 법선·텍스처 | Rocks는 Icosahedron+ballNormals와 높이별 평균 색 띠 | §6.3의 최종 목표와 차이. 대표 바위 앞·옆·위 close-up 및 의미별 recipe로 처리 |
| 수면의 층/연결 영역 구분·해안 감쇠 | 물 칸 네 개 최소 높이, x*4096+z 공유, LIFT=.09; 해안 감쇠 없음 | 기존 파고 관통 수정은 인정. 다른 높이 수면의 인접/중첩 및 기슭 상승을 별도 검사. 근거 없는 ‘전 수면 완료’ 금지 |
| 카드 전체의 의미별 최종 판정 | 카드 후보표 미검수0과 coverage의 unreviewed는 서로 다른 집계 | 후보표와 전체 sourceParts 모집단을 구분한 coverage 보고 필요 |

이번에 읽은 `.audit/first-person/coverage.json`은 2026-09-18 생성이며 sourceParts 284,314, verified 1,636, unreviewed 282,678이다. **282,678개 결함이라는 뜻이 아니다.** 일반 원본 면도 포함한다. 반대로 좁힌 후보표 미검수0으로 모든 원재료가 검수됐다고 주장할 수도 없다.

수정 LLM은 전체 명세 §6~10 각 항목을 `구현됨/의도적 유지/검증대기/미구현`으로 매핑하고 이유와 현재 evidence를 남긴다. 의도적인 평면 장식은 유지해도 된다. 나무/바위를 한꺼번에 다시 만들기 전에 현재 화면과 기존 목표의 차이를 대표 자산으로 입증하고, 필요한 종류만 수정한다. 물은 같은 높이/층/연결 영역에서만 공유하고 해안에 밀착해야 한다. 임의로 전체 lift를 더 올려 해결하지 않는다.

Scale2x 64 목표는 새 원본 디테일을 복원하지 않는다. 원본에 64×64가 있다는 사실도 world texel density가 같다는 근거가 아니다. 현재 정책은 실용적인 확대 선택으로 기술하고, x2/x4의 정적 품질·장면 전환 누적 메모리를 비교해 결정한다.

### R7 — P2: 회복 주머니에서 약이 첫 줄이 아니면 하네스가 사용을 포기한다

근거: 최신 `JOURNEY_BADGE2_20260916.md §13.14`, `tools/e2e/drive.mjs:maybePotion`의 `mine.row !== 0` 분기. 약 수량이 있어도 row가 0이 아니면 `potion.left=0`으로 남은 사용을 포기한다. 첫 줄의 다른 도구를 잘못 쓰지 않는 보호는 유지한다. 이 문제는 제품 가방에서 약이 사라진 것으로 확정된 문제가 아니다.

수정 절차:

1. 관장 직전 저장 상태와 실패 순간의 회복 주머니 순서를 확보한다. 항목 ID·이름·수량·battle pocket 필터 결과·화면 선택 index와 스크린샷을 함께 기록한다. 첫 항목이 사탕이라는 설명은 아직 가설이므로 확정하지 않는다.
2. `obs.bagState().items[].row`가 실제 전투 가방의 필터/정렬 결과와 같은지 확인한다. 저장 가방의 순번을 화면 줄 번호로 바로 쓰지 않는다.
3. 실제 UI에서 목표 도구 ID에 해당하는 줄을 찾아 방향키로 이동하고 선택 항목 ID가 일치하는지 확인한 뒤 결정한다. 현재 키 입력 경로를 유지한다. 필요하면 접근성 속성 또는 read-only `data-item-id`/선택 상태를 추가하되 스토어로 직접 소비하지 않는다.
4. 목표가 없거나 다른 메뉴/교체 질문이면 입력을 멈추고 사유를 기록한다. 약이 두 번째라는 이유만으로 영구 포기하지 않는다. 같은 턴 입력 중복 방지는 유지한다.
5. 사탕 잔량을 무조건 삭제해 약을 첫 줄로 만드는 우회는 채택하지 않는다. 실제 중복 지급/소비/0수량 필터 결함이 확인된 경우 해당 원인을 별도 수정한다. 정상 가방 내용과 순서는 존중한다.

완료 시험: 목표 약이 0/1/2번째인 경우, 앞에 비전투 도구 또는 다른 회복 도구가 있는 경우, 소비 후 목록이 줄어드는 경우, 교체 질문 중 호출. 올바른 약만 1개 줄고 HP가 맞게 증가하며 다른 도구는 유지돼야 한다. 관장전 재현에서 구매/사용/잔량과 사용하지 않은 이유를 각각 보고한다. 단순 여행 PASS만으로 회복 경로까지 통과했다고 하지 않는다.

## 4. 수정 순서와 공통 설계

실행 순서: **R1 공통 시계 → R2 포획/등판 대기 → R3 HP/기절 → R7 약 선택 하네스 → R5 공간 결함 → R6 전체 범위 마감 → R4 mirror 잠재 오류 → 최종 회귀**. R7은 최종 journey 재검증 전에 끝낸다.

R1~R3은 한 presentation 계약으로 설계하되 리뷰 가능한 변경 단위로 나눈다. battle controller가 확정한 사건 순서는 보존한다. 글과 시각 효과를 나타내는 Beat에 명시적인 완료 조건을 추가하고, 전투 중 새 이벤트가 붙어도 이미 재생한 prefix가 달라지지 않는 기존 불변식을 유지한다. 상대/내 쪽/더블 slot을 seq와 함께 식별한다. 폴백 자산과 네트워크 지연도 완료 조건의 일부다.

기존 폼 변화는 protocol/view에 구현되어 있다. BattleStage의 ‘formechange가 뷰에 안 올라온다’ 주석만 보고 기능 미구현으로 되돌리지 않는다. 주석과 최신 구현을 대조하고 낡은 설명은 정리한다.

## 5. 필수 통합 회귀와 완료 기준

### 5.1 배틀 순서표

모든 기술을 사람 눈으로 한 번씩 봤다는 근거가 없으면 ‘배틀 모션 전부 완료’라고 보고하지 않는다. 전체 지원 기술은 자동 timing/source coverage를 만들고, 의미가 다른 연출군은 실제 영상으로 검증한다.

| 장면 | 최소 검증 |
|---|---|
| 야생/트레이너/더블 등장 | 배경 준비 → 등장 → 볼 열림 → 몸 → 메뉴; 내/상대 순서, preload 실패 |
| 물리·특수·상태·자기 강화 | 기술 글 → 몸/VFX → 피격 → HP → 급소/상성/부가 글 |
| 연속 공격·광역·빗나감·무효 | 타수별 HP/반응, 올바른 slot, 빗나간 대상에 피격 금지 |
| 충전/공중날기/구멍파기 | 사라짐과 복귀, 다음 턴까지 남아야 하는 상태 |
| 교체·강제교체·기절 | 회수/퇴장 완료 → 새 공 → 새 몸; 오래된 clip/VFX 초기화 |
| 포획 성공/실패·사파리 | R2 전 사례, 결과 뒤 UI·저장·다음 턴 |
| 날씨·독·반동·동시 KO | 메시지/게이지/기절/승패 순서, 더블 양쪽 처리 |
| 경험치·레벨·기술·진화 | 배틀 종료와 파티 HP/폼/기술 일치, 선택 중 멈춤 |

30/60/120/144Hz 논리 시뮬레이션과 실제 WebGL/WebGPU 대표 영상을 별도로 제공한다. auto/A연타/배틀 속도 전 설정을 확인한다. 이벤트 seq, beat index, logical time, VFX begin/end, HP begin/end, 메시지 최초 표시, sceneReady, 전투 phase를 하나의 trace에 담는다. trace assertion으로 확인한 순서와 사람이 확인한 화면을 구분한다.

### 5.2 시점 및 게임 진행

- 1→3→1 반복 20회: 위치·높이·yaw/pitch 정책·자기 몸 visibility·입력 기준 복원, 포인터 잠금과 메뉴 닫기.
- 문 출입/워프·계단·갈색 턱 세 방향·자전거·surf·낚시·동굴·깨어진 세계: 양 시점 이동과 토글. collision과 화면 mesh를 따로 확인.
- 1인칭에서 전투 진입 → 기술/포획/승리/도주/전멸 → 필드 복귀 시 원래 옵션·카메라·플레이어 visibility 복원. 별도 3인칭 시작 판도 실행.
- 저장/불러오기, provider 교체, 맵 왕복 10회에서 stale geometry·재질 누락·메모리 누적·빈 인스턴스 draw 확인.
- 기록용 journey는 현재 정본 계약의 새 게임과 고정 source/harness/data digest로 실행. 재개 시 커밋 `09e77d7`은 사탕 레벨 맞추기를 기본 흐름으로 바꾸고 shortcuts=[]로 기록한다. 문서에 사람이 정한 변경이라고 명시돼 있으므로 이전의 무사탕 정책으로 되돌리지 않는다. 대신 이 판이 자연 레벨링 난이도 검증은 아니라는 범위를 명시한다. 좋은상처약 미표시 진단도 수집. 이번 짧은 확인 지점 검사가 journey 완주를 대신하지 않는다.

### 5.3 증거 유효성

**과거 직접 실행 기록:** `pnpm release:check`는 종료 코드 1이었다(`.audit/review-20260919-release.log`). 당시 판정의 blocker는 **1건: journey가 검사한 source digest `5c3bdc731327`과 현재 `c652443121e4`가 다름**이다. 검사 초기에 읽은 이전 release-blockers 파일에는 여러 건이 있었으나 이번 판정 결과로 갱신해 읽는다. blocker 문구는 아직 첫 배지라고 하지만 실제 journey 계약은 둘째 배지까지 확장되어 있으므로 현재 정본 roster 전체를 재실행해야 한다. 이는 현재 게임이 실제로 완주에 실패했다는 뜻이 아니라 최신 코드의 유효한 완주 증거가 없다는 뜻이다. 최종 수정 후 판정기를 다시 실행한다.

**6ec7fbd 후속 직접 실행:** `pnpm release:check`는 다시 종료 코드 1이며 차단 **7건**이다(`.audit/review-20260919-postcommits-release.log`). 기존 차단 1건을 최신 상태로 사용하지 않는다.

| 판정 항목 | 현재 저장된 증거의 제한 |
|---|---|
| 실제 호스트 | 호스트 측정 build b8973b4와 로컬 dist의 09e77d7-dirty가 다름 |
| 브라우저 suite | 실제 호스트 CSP 항목 BLOCKED 1건 |
| story | 현재 증거 파일에 90개 중 87개 미실행; 부분 재측정 결과를 전체 실행으로 합치지 않음 |
| 빌드 식별 | 기존 dist가 09e77d7-dirty로 만들어짐 |
| GPU loss / first frame / journey | 기록된 source c652443121e4와 현행 판정 값 7500aeea29c5가 다름 |

제품 `src/`가 같아도 Vite/배포 설정 등 판정 대상 변경으로 바인딩 값이 달라질 수 있다. 불일치를 곧바로 렌더링 회귀라고 부르지 않는다. 기존 dist를 새로 구운 것으로 가정하지 말고 최종 수정 커밋에서 build 후 필요한 suite와 실제 호스트 증거를 다시 맞춘다. 이번에는 이 판정을 확인했을 뿐 build/배포/전체 suite는 실행하지 않았다. 최신 journey PASS 23도 기존 유효성 검사를 우회해 채택하지 않는다.

최종 결과는 다음을 모두 제공해야 한다: 고정 commit/source/harness/data/build ID, check 결과와 skipped 이유, 두 backend 확인, 영상/trace/이미지 경로, 전체 scope coverage 및 예외. 정본 계약에 포함되지 않은 진단 판을 배포 성공으로 옮기지 않는다. 현재 기본 사탕 사용은 새 journey 계약으로 평가하되 과거 --candy 진단 결과를 소급 승격하지 않는다. 한 색/자홍/오류 수치만 낮추거나 검사 threshold를 느슨하게 해 완료 처리하지 않는다. 통행 가능한 시점의 품질 결함과 진단 전용 위치의 관찰을 구분한다.

## 6. 수정 LLM에게 전달할 요약 지시

이 문서의 R1~R7을 §4 순서대로 처리하라. 시작 전에 현재 HEAD와 작업 트리를 기록하고 다른 작업을 보존하라. 진단용 `.audit` 파일이 없더라도 이 문서의 재현 조건으로 테스트를 재작성할 수 있어야 한다. 먼저 R1~R3을 함께 설명하는 공통 presentation timeline을 설계하되 실제 사건 계산은 변경하지 말라. 실제 source를 사용하는 실패 테스트를 만든 뒤 수정하고, 의미 있는 경계/순서 검증을 수행하라. 정적 통과와 실제 플레이 통과를 구분하고 미실행은 미실행으로 남겨라. 마지막에는 네 건 개선 완료가 아니라 전체 명세의 coverage와 미해결 항목을 보고하라.
