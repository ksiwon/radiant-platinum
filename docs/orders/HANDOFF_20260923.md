# 인수인계 (2026-09-23 11:40 · 대표 구간 3판을 중간에 끊은 자리)

이 파일은 **이어받는 사람이 처음부터 다시 재지 않아도 되게** 하려고 쓴 것이다.
고친 것과 못 고친 것을 나누고, 각각 **판으로 확인했는지**를 못 박았다.
「고쳤다」와 「고쳤다고 본다」는 다른 말이다.

---

## 0. 지금 상태 한 줄

작업 트리에 **커밋 안 한 변경 24파일**(+1,985 / −117)이 있다. 시험 4,880개 ·
`tsc` · `lint` · `exports` 전부 초록. **푸시·배포는 안 했고, 하지 말 것.**
배포판은 아직 `2cfdc7c`다.

### 지켜야 하는 제약 (사용자가 못 박은 것)

- **배포·푸시 금지.** 지금 배포 버전은 확실하지 않으면 그대로 둔다.
- 측정할 때 `warpTo`·`window.pt`·세이브 편집 금지(진단용 `pnpm shot`은 예외).
  방향키/A/B/화면 버튼만 쓴다. `vars`·`flags`·`bag`·`badges`는 개발 서버
  모듈에서 **읽기만**. 사탕은 개발 모듈로 **가방에 넣을 수만** 있고 **먹이는
  것은 화면**으로. 기록용 판은 새 게임부터, 깃발 없이.
- `rm -rf`·`reset --hard`·`stash` 금지. 남의 node 프로세스 안 죽인다.
- 판이 도는 동안 `src`를 고치지 않는다 — HMR로 섞여 그 판의 기록이 못 믿을
  것이 된다.

---

## 1. 이번에 **고쳤고 판으로 증명된** 것

### 1.1 3인칭에 제자리 돌기가 없었다 — 장막 체육관을 **사람도 못 깼다**

`src/engine/actor/player.ts`. 충돌이 속도를 0으로 만든 뒤 얼굴 갱신이 건너뛰어,
**막힌 쪽으로는 영영 못 돌았다.** 원작 `PlayerAvatar_UpdateMoveState`는 막혔는지
묻지도 않고 돈다(`AVATAR_MOVE_STATE_TURNING`).

```ts
} else if (desired.lengthSq() > 0.01 && !isSliding()) {
  p.facing = surfaceHeading(frame, desired.x, desired.y, desired.z, p.facing)
}
```

**증명** — 장막 체육관 9판 2/12 → 10판 8/12 → 11판 **12/12 · 배지 4**.
시험은 `src/engine/actor/turnInPlace.test.ts`(신규 5개).

> 이게 아홉 판을 하네스 탓으로 돌리다 열째 판에 잡힌 것이다. 재는 자가 같은
> 물리 동작에서 여러 판 서면 **제품을 의심**한다(REPAIR §59·§60).

### 1.2 들판 체육관이 **아무에게도 안 열려 있었다** — 원작 함수를 반쪽만 옮겼다

`src/scene/pastoriaGym.ts`. 두 줄짜리 원작 함수의 둘째 줄을 안 옮겨서 물이
주황(낮음)에서 시작했다.

```c
PersistedMapFeatures_InitWithID(pmf, DYNAMIC_MAP_FEATURES_PASTORIA_GYM);
feature->pressedButton = PASTORIA_GREEN_BUTTON_PRESSED;   // ← 안 옮긴 줄
```

`const pressed = PASTORIA_BUTTON.green`로 고치니 **단추 한 번에** 풀린다.
**증명** — `past42-4.log`: 단추 1번 밟고 맥실러 앞 → **배지 5**.

### 1.3 실내 문이 **벽으로 덮여 있었다**

`src/scene/roomWalls.ts` + `src/scene/ChunkModels.tsx`. `DOOR_OPEN = 2.25`를 두고
문간 칸은 **인방만** 남긴다. 임자는 `--blame`으로 확정했다(우리 「방 벽」이
칠했고 그 뒤는 `#000001` — 원작엔 아무것도 없다).

### 1.4 방 벽이 **원작 벽 앞에 한 장 더** 섰다 (파일럿 ⑤)

같은 파일. 「메운 구멍」 갈래가 원작 덮개를 무시하고 바닥부터 꼭대기까지
세우고 있었다. `gapsOn`으로 **안 덮인 띠만** 세운다. 빈틈 수 불변(9/15/10/0/0).

### 1.5 대표 구간이 **축복시티에서 통째로 무너졌다** — 막은 것은 프레임이다

`tools/e2e/drive.mjs`. `makeStall`·`makePen`이 **바퀴**를 세는데 한 바퀴는
벽시계다. 화면이 4~8프레임/초로 기면 걸음이 반 칸도 못 가고, 밖에서는
「120바퀴를 칸 4개 안에서만 걸었다 = 되밀린다」로 보인다. **못 가는 것과 안
가는 것이 같은 모양이 된다.**

`CRAWL_FPS = 15` 아래인 바퀴는 **안 센다**(`goTo`·`stepOn` 둘 다). 총예산은
그대로라 정말 막힌 판은 여전히 예산에서 끝난다.

**증명** — 1판은 여기서 죽었다(스쿨 못 감 → 광대 숨김 깃발 안 풀림 → 쿠폰 0/3
→ 포켓치 없음 → 동쪽 잠김 → 무쇠시티·탄광·체육관 전부). 2·3판 연속 통과.

### 1.6 험한 샛길(254)에 **들어가면 못 나왔다** — 「지나온 문」 규칙이 방을 잠갔다

`tools/e2e/drive.mjs` 마지막 갈래. 254는 문이 둘인데 앞문 (28,44)은 깰 바위
너머고 들어온 문 (19,50)은 `usedDoors`가 뺐다. 두 문 사이를 영영 오가지
않으려던 규칙이 **방을 통째로 잠갔다.**

```js
const fresh = leads.filter(
  (w) => !usedDoors.has(`${String(s.map)}:${String(w.x)},${String(w.z)}`))
const back = fresh.length > 0 ? fresh : leads
```

**증명** — 1판은 `254의 (18,50)에서 길을 못 찾았다 (unreachable)`가 11번.
3판은 `바위 (23,44) → 치웠다 · 길이 열렸다` 뒤 `지나온 맵 [6,3,345,254,346]`.

### 1.7 영원 체육관에서 **못 나왔다** — 이긴 뒤 꽃시계를 다시 안 읽었다

`tools/e2e/journey.mjs`. 벽을 싸우기 **전에만** 읽었다. 관장을 이기면 시계가
한 번 더 도는데(`ETERNA_CLOCK.gymLeader`) 그 뒤 읽는 자리가 없어서 나갈 때
**한 상태 낡은 벽**으로 길을 냈다. 벽은 줄기만 하는 게 아니라 **앞 상태에
열려 있던 칸이 막힌다.**

**증명** — 2판은 배지 2 딴 뒤 (10,13)에서 90바퀴를 한 칸도 못 갔다.
3판은 `관장 유채 재도전 → 배지 2개 · 시계 4` 뒤 그대로 빌딩 1F·2F·3F로 갔다.

### 1.8 그 밖

| 무엇 | 파일 | 무엇이 틀렸나 |
|---|---|---|
| 배틀이 **말없이 멈출 수 있었다** | `state/battleStore.ts`, `ui/battle/BattleScreen.tsx` | 터지는 것과 안 돌아오는 것이 같아 보였다. `LOADING_TELL_MS = 20_000`, 어디서 멈췄는지(`규칙기`/`게임 자료`/`파티`/`심판`)를 화면과 콘솔에 |
| 사탕이 **지진을 잊게 했다** | `tools/e2e/drive.mjs` | 기술 교체 물음의 기본 답이 「0번을 지운다」였다. 기본을 `'refuse'`로 |
| 사탕이 **진화 뒤 마리를 못 찾았다** | `tools/e2e/journey.mjs` 등 | 찌르꼬(397)만 찾아 찌르호크(398)를 놓쳤다. 계통 배열(`STARLY_LINE`)로 |
| 들판 레벨이 모자랐다 | `tools/e2e/journey.mjs` | 선두 39→46, 둘째 36→43, 셋째 43 신설, 좋은상처약 12개 |

---

## 2. 대표 구간 3판이 **어디까지 갔나** (11:34에 사람이 끊음)

기록: `.audit/journey/journey-full3.log` (169줄)

| 자리 | 시각 | 결과 |
|---|---|---|
| 새 게임 시작 | 09:50경 | — |
| 축복시티 도착 | +5분 30초 | ✅ 1판이 죽은 자리 |
| 스쿨·쿠폰 3/3·포켓치·동쪽 통행 | | ✅ 전부 |
| **배지 1 (로안)** | 10:51 | ✅ **51분** (어젯밤 기록 48분과 비슷) |
| 험한 샛길(254) 바위 | | ✅ 깨고 지나감 |
| **배지 2 (유채)** | 11:25경 | ✅ 한 번 지고 재도전해 이김. **43분** (어젯밤 93분의 절반 아래) |
| 영원 체육관 **나가기** | | ✅ 시계 4로 갱신된 벽으로 |
| 갤럭시 빌딩 1F·2F·3F | 11:34 | 3F 트레이너 도는 중 **← 여기서 끊음** |

세이브는 `.audit/journey/seg-NN.rpsave`로 남아 있다(최신 `seg-23`). 이어
달리려면 `node tools/e2e/journey.mjs --from=23` — 다만 **소스가 바뀌면 다이제스트가
달라져 처음부터 다시 돈다**(`skip-only-by-digest`). 그게 맞는 동작이다.

### 아직 **한 판으로** 못 간 구간

배지 3·4·5는 **세이브에서 각각 한 판씩** 깼다(기록 있음). 새 게임에서
**한 판으로 이어서** 다섯째까지 간 적은 아직 **없다**. 3판이 그것을 하려던
판이었고 배지 2까지 갔다.

---

## 3. **아직 못 고친** 것

### 3.1 파일럿 ⑦ — 2층에서 내려가는 계단이 판때기다 · **방침만 정해졌다**

주인공 방(맵 415)의 내려가는 계단은 **계단 그림이 그려진 빨간 판 한 장**이
바닥에 납작하게 깔린 것이 전부다. 3D 단이 없고 그 칸의 바닥 높이도 안 내려간다.
**원작 방 모델에 있는 것이 그 판뿐이다** — 고치려면 원작에 없는 것을 세워야 한다.

> ⚠️ 올라가는 계단(집 1층 맵 414)은 멀쩡하다. 파일럿도 그쪽은 문제없다고 했다.

**2026-09-23 사용자가 골랐다 — 「입구만 세운다」.** 바닥·충돌 자료는 안 건드리고
난간 + 두세 단만 세우고 그 아래는 어둡게 묻는다. 원작 충돌·높이 자료를 하나도
안 건드려서 다른 방으로 번질 위험이 없다.

**착수 안 한 까닭** — 판이 도는 동안 `src`를 고치면 HMR로 섞인다. 판을 끊었으니
지금은 착수해도 된다.

### 3.2 파일럿 ⑥ — 의자·방석이 판때기다 · **사람 승인이 필요하다**

짚어는 놨다. 포켓몬센터(맵 420) 네 귀퉁이의 방석이 **소품** `pc_s05a`·`pc_s05b`고
**물건**이라 「평평한 것이 맞다」의 갈래가 아니다. 낮은 둥근 상자 기하 + 레시피를
넣으면 되는데, `visual/recipes.ts`의 레시피는 **사람 승인(`verified`)이 있어야
게임에 걸린다**(`draft`는 candidate 모드 전용). 그래서 그림을 찍어 사용자에게
보이고 승인을 받는 절차가 남았다.

**방 기하 쪽은 괜찮다** — 실내 행렬 271갈래를 훑으니 오려 낸 판 1,012조각 중
서 있는데 평평한 것이 **39개**뿐이고 219개는 두께를 얻는다
(`.audit/probe/flatPlatesIndoor.mjs`).

### 3.3 파일럿 ① — 첫 배틀이 배경만 뜬 채 멈춘다 · **재현 못 했다**

한국판 설치본에서 게이트 ㉖ **PASS**(야생 1 · 트레이너 1 · 콘솔 오류 0 · 프레임
중앙값 60). 남은 차이는 **파일럿의 낡은 OPFS 설치본**이거나 **그 기계의
GPU/브라우저**다. 이제 20초를 넘기면 화면에 어디서 멈췄는지 뜨니(§1.8),
**사용자 콘솔 한 줄이면 끝난다.** 그 한 줄을 아직 못 받았다.

### 3.4 203번도로에서 다섯 번 전멸했다 — **결함은 아니지만 느리다**

3판에서 `배틀 뒤 맵이 344→6로 바뀌었다 — 전멸해서 되돌아왔을 수 있다`가 다섯
번. 레벨이 낮아 지는 중이고 하네스가 센터로 돌아가 낫고 다시 가서 결국 뚫는다.
진행에는 지장이 없지만 203번도로에서만 **6분**을 먹는다. 사탕으로 레벨을 맞추는
자리를 여기에도 하나 두면 줄어들 것이다(`journey-levels-by-candy`).

### 3.5 손 안 댄 것

- 맵 **353**에서 「나갔다 다시 들어올 문」이 `unreachable` — 천관산 입구 길찾기.
  254와 같은 갈래인지 **안 봤다**. 254 고침이 353도 같이 풀었을 수 있으나 확인
  못 했다.
- 커밋을 아직 안 했다.

---

## 4. 이어받는 사람이 할 일 (순서 제안)

1. **커밋한다.** 24파일이 떠 있다. 푸시는 하지 말 것.
2. **⑦ 계단을 「입구만」으로 세운다.** 방침은 정해졌고 브라우저도 비었다.
   `pnpm shot`으로 맵 415를 찍어 눈으로 확인한다.
3. **⑥ 방석** — 기하 + `draft` 레시피를 만들고 candidate 모드로 찍어
   **사용자 승인**을 받는다. 승인 없이 `verified`로 올리지 않는다.
4. **대표 구간 4판**을 새 게임부터 끝까지 돌린다. 배지 2까지는 세 판 연속으로
   되는 것이 확인됐으니, 이번엔 **배지 3·4·5**가 관심사다.
   `node tools/e2e/journey.mjs --budget=20000 2>&1 | tee .audit/journey/journey-full4.log`
   (오래 도는 판은 `tee`로 흘리고 `tail -f`로 본다 — `| tail`로 걸면 끝날 때까지
   0바이트다)
5. 353 길찾기를 본다.

---

## 5. 읽을 것

| 무엇 | 어디 |
|---|---|
| 이번에 고친 것들의 **근거와 실측** | `docs/REPAIR.md` §60~§66 |
| 파일럿 보고 ①~⑦ 전문과 **안 한 것의 까닭** | `docs/orders/PILOT_REPORTS_20260922.md` |
| 셋째~다섯째 배지 지시서 | `docs/orders/JOURNEY_BADGE345_20260922.md` |
| 3판 기록 | `.audit/journey/journey-full3.log` |
| 1판 기록(축복시티에서 죽은 판) | `.audit/journey/journey-full.log` |
| 배지 5를 세이브에서 깬 기록 | `.audit/journey/past42-4.log` |
