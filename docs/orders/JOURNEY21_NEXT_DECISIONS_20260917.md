# journey21 구현 보고 후속 결정 — 2026-09-17

## 근거와 범위

미커밋 작업 트리와 RARE_CANDY_20260917.md, JOURNEY_BADGE2_20260916.md §13.8~13.9, FIRST_PERSON_FP_20260917.md를 대조했다. rareCandy.ts, 진화 경로 diff, 증거 판정 및 구간 신원 구현을 읽었다. 전체 코드 리뷰·화면 재검증은 수행하지 않았다. 시험 4,669개 및 journey 결과는 구현 보고에 따른 값이며 이번 기획 검토에서 재실행한 결과가 아니다. 이 문서는 후속 작업 지시이고 커밋·장시간 journey·배포를 실행했다는 뜻이 아니다.

## 1. 기록용 판 시점

필요한 제한 검토와 UI 가림 수정 → 관련 변경을 검토 가능한 커밋으로 확정 → 깃발 없는 새 게임 전체 journey 순서로 한다. 미커밋 트리의 판은 진단 자료로만 유지한다. 다른 작업의 변경까지 일괄 stage/commit하지 않는다. 실행 대상 소스·하네스·데이터의 식별값을 고정하고 시작/끝 digest를 대조한다. 실행 중 관련 파일이 바뀌면 해당 결과를 확정 기록으로 승격하지 않는다.

로컬 커밋에서 통과한 것과 배포된 빌드의 통과는 구분한다. DEPLOY.md ⑪은 해당 배포 artifact/buildId를 실제로 검사하고 기존 evidence 계약을 만족했을 때만 갱신한다. 커밋했다는 이유만으로 개발 서버 판을 배포 증거로 옮기지 않는다. 사탕 판 세이브·shortcuts 누락 봉투는 기록용 판에 사용하지 않는다.

## 2. 사탕 판에서 찌르꼬 미포획

**(가) 기록하고 계속 진행**을 채택한다. 추가 포획이나 선두 L28 보정은 이번 계약에 넣지 않는다. 결과에 미포획, 찌르꼬 사탕 단계 미실행, 실제 파티/레벨을 남긴다. 여행 전체 진행 결과와 찌르꼬 사탕 시나리오 실행 여부를 분리한다. 유채 승리를 찌르버드 효과로 해석하지 않는다.

찌르꼬 사탕·기술·진화 경로의 검증은 별도 고정 세이브 진단으로 확보한다. 네 기술이 찬 상태의 교체/거절/다음 기술 진행은 기존 _candy42 성공만으로 확인됐다고 하지 않는다. 정상 기록용 journey에는 개발 도구로 사탕/포켓몬을 넣지 않는다.

## 3. 진화 HP 수정

공통 진화 경로 수정은 유지하되 **기록용 판 전 별도 집중 검토**를 필수로 한다. 사탕에만 한정하면 배틀 뒤 진화 결함이 남는다. 다만 레벨업 사탕의 revive 효과와 진화의 능력치 재계산은 서로 다르다: 진화만으로는 HP 0이 되살아나지 않는다. 현재 carriedHp도 이를 구분한다.

검토 항목: evolve의 모든 호출부, 폼을 반영한 전후 종족, 가득/부상/기절 HP, 껍질몬과 추가 생성 경로, 진화 취소 시 무변경, 진화 확정 시 한 번만 반영, 배틀 뒤 파티와 저장 데이터의 일치. 기존 단위 시험을 활용하고 누락된 의미 있는 경계 사례만 보완한다. 실제 배틀 뒤 진화 1회와 사탕 뒤 진화 1회에서 전후 종족·최대HP·현재HP를 기록한다. 사탕 진화 성공만으로 배틀 뒤 경로를 검증했다고 하지 않는다.

## 4. 레벨업 능력치 창

현재 선두 카드 가림은 수정 대상으로 확정한다. 구현자는 로컬 원작 party menu 창 정의/자료와 현재 screenshots를 대조해 실제 위치 근거를 기록한다. 아직 원작 위치를 확인하지 않았으므로 특정 픽셀 좌표를 기획에서 추정하지 않는다.

우선 계약: 대상 포켓몬의 이름/레벨/선택 상태와 진행 안내가 가려지지 않아야 한다. 기존 UI 크기 체계 안에서 별도 영역 또는 적절한 모달 배치를 택하고 지원 최소 화면·일반 화면에서 검수한다. 원작과 현재 화면 구조가 달라 동일 좌표를 사용할 수 없으면 차이와 배치 이유를 적는다. gain/value 두 단계, 긴 한국어 이름, 6마리 파티, 기술 교체 화면 전환, 진화 진입을 확인한다. 표시 순서와 A/B 진행 규칙은 유지한다.

## 5. 다음 1인칭 순서

1. **자홍 156삼각형:** 보고된 수치를 현재 inventory로 재확인하고 source/UV/palette/굽기 경로를 분류한다. 누락 원인을 고치며 자홍 색만 감추거나 원본을 삭제해 수치를 0으로 만들지 않는다. 전체 표본 재검사 및 대표 배치의 양 backend 캡처를 완료한다.
2. **볼라드 두 갈래·343배치:** 두 갈래의 실제 의미·source·기하 차이를 확정하고 별도 recipe로 처리한다. 기반부/끝단/그림자/통행 공간, 원본 제거와 교체 준비의 원자성을 확인한다. 현재 배치 수로 재집계한다.
3. **갈색 한 방향 턱:** FIRST_PERSON_VISUAL_IMPROVEMENT_PLAN.md §7.4의 FP-LEDGE를 수행한다. 이전에 사용자가 추가한 필수 범위이므로 빠뜨리지 않는다. 세 방향·연속 구간·끝단·실제 점프 검증을 완료한다.
4. **평평한 컷아웃 카드 23,360개:** 숫자만큼 일괄 입체화하지 않는다. 모든 배치를 고유 source/의미/변환별로 분류하고 keep/augment/replace를 정한다. 고빈도·가까이 접근 가능·실루엣 소실이 큰 환경물부터 처리한다. 의도적인 평면은 근거를 남긴다. 보고 수치는 현재 inventory로 재검증한다.

위 순서는 시각 자산 복구와 작업 규모에 따른 기획 우선순위이며 최신 화면에서 모든 결함을 직접 확인했다는 뜻이 아니다. 기존 전체 명세의 준비/폴백/층/성능/실제 first 모드/두 backend 기준을 따른다.

## 6. 좋은상처약 미표시

미해결 상태를 유지한다. 단독 재현 네 번 성공은 UI 결함을 배제하지 못하며 하네스 원인도 확정하지 못한다. 다음 긴 판만 기다리지 말고 기존 실패와 가까운 전투 세이브에서 반복 진단을 먼저 시도한다.

miss 순간에 스크린샷, 현재 화면/메뉴·탭·선택 index, 도구 ID/수량과 표시 목록, 전투 턴·입력 잠금, 직전 입력/시각을 함께 남긴다. 재시도 직전 화면 상태를 확인하고 같은 턴에 입력을 누적하지 않는다. 성공 시 수량 감소와 HP 변화를 확인해 늦게 처리된 입력의 중복 사용을 구별한다. 세 번 제한은 재시도 정책이지 수정 완료 판정이 아니다. 재현되지 않으면 미해결로 남긴 채 고정 커밋의 기록용 판에서 자료를 수집할 수 있다. 진행 성공과 이 결함의 해결 여부를 따로 보고한다.

## 7. nul 파일

이번 기획 작업에서는 삭제하지 않는다. 생성 경로와 실제 내용/소유 작업을 확인한 뒤 별도 정리한다. 관련 기능 커밋에 포함하지 않는다.
