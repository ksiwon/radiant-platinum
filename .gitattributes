# 확인 지점 세이브 (PLAN §15.1b) — 줄 끝을 손대지 않는다.
#
# 굽는 자가 LF로 쓴다. `core.autocrlf`가 켜진 기계에서 CRLF로 바뀌어도 봉투는
# 열리지만(체크섬은 `payload` 한 줄만 덮는다) **기계마다 파일 바이트가 달라져서**
# 「굽고 나서 바뀐 게 있나」를 못 묻는다. 그 물음이 이 파일들의 쓸모다.
saves/*.rpsave -text
